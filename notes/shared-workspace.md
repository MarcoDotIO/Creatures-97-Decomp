## Shared Workspace

### Repo Organization Note
- 2026-04-22: additive repo organization work is now live. New navigation docs are under `docs/`, the additive CMake surface is rooted at `CMakeLists.txt` and `cmake/`, and Windows guest helpers now live in `scripts/check_windows_vm_build_tools.sh` plus `scripts/run_cmake_in_windows_vm.sh`. The committed Windows VM compiler wiring is now captured under `cmake/toolchains/` and the `windows-vm-*` CMake presets; nothing under `reconstruction/` was relocated. The authoritative reconstruction source paths did not move: keep using `reconstruction/engine/`, `reconstruction/creatures_slice/`, and `reconstruction/apps/` exactly as before.

### Active Tasks (In Progress):
- `codex-creatures-target-proximity-helper-band`: Recover the narrow Creatures-local target proximity helper seam around `FUN_0041fe30`, `FUN_0041fff0`, and `FUN_004200b0` into a focused module next to the recovered target-scan and target-slot code. Scope stays on those three predicate-style helpers only, and excludes the already recovered target-scan loop, target-slot matrix, selection-focus, imported-creature, startup preview, Launcher, Science, Breeder, Health, and embedded-object archive bands.
- `codex-embedded-object-archive`: Recover the first live `World.sfc` embedded-object deserializer layer around `FUN_00413410`, `FUN_004136f0`, `FUN_00413730`, `FUN_00413b80`, and the first anchored `PointerTool` / `Entity` / `CompoundObject` payloads into a focused archive module plus bootstrap output/tests. Scope excludes the `MapData` runtime/weather helpers claimed above. The first live `PointerTool`, `Entity`, `CompoundObject`, following `SimpleObject`, following `Vehicle`, following `Blackboard`, following `Lift`, and following `CallButton` records now all decode through the recovered bootstrap path; the parser now also separates the top-level `Entity` body from the smaller inline `ReadObject(Entity)` payload used under `SimpleObject` / `CompoundObject`, and the `CallButton +0x54` arm is now modeled as the original `ReadObject` tag instead of a raw `u16`. A focused partial `FUN_0040e090` creature-pose/body-layout archive module now exists, and its root/appendage reference arms are now modeled through the dedicated `FUN_0041a060` / `FUN_0041a8d0` record shapes. The live post-`CallButton` pose probe still fails, but the stream immediately after the decoded `0x10c9d` part record is now typed more tightly as `u32 0` plus a two-entry classifier-script catalog ending at `0x10d0b`, followed by a stable 30-byte `outer=0x8008 / inner=0x0134` bridge record that now parses end-to-end with the first repeated family as `post_callbutton_bridge_family`. The later `0x10efb` corridor is now verified as `u32 3` plus three wrapped chain records ending at `0x10f50`, and the repeated family band after the first `0x10d29` start is now typed across eight live starts (`0x10d29`, `0x111d0`, `0x115ea`, `0x11b1e`, `0x12052`, `0x1246c`, `0x129a0`, `0x12e47`) as a stable lead-part/catalog/chain-list/body-table prefix with a variable scripted tail of two to four `part + zero + two-script catalog` records plus the same 30-byte bridge between families. The frontier now moves several bands deeper too: the bytes after the last repeated-family bridge at `0x13243` decode as `part -> one-entry classifier-script catalog -> two-entry typed-chain list -> pair table -> follow-up part -> three-entry classifier-script catalog` across `0x13261`, `0x1332f`, `0x133ad`, `0x13408`, `0x13480`, and `0x134e5`, then continue as a verified tail series of fourteen `typed_chain -> part -> classifier-script catalog` nodes from `0x136eb` through `0x15038`. The tail splits into two live phases: nodes `0..5` use `outer=0x8008`, `inner=0x018d`; nodes `6..13` switch to `inner=0x019a`. The first ten tail nodes keep `ref=0x8011`; later nodes continue the same outer-chain shape but now reach `ref=0x803c` with a 12-script catalog keyed `0x0000000a` at `0x15038 -> 0x154e4`. The next band after that tail is now decoder-pinned as `part -> six-entry classifier-script catalog -> three-entry typed-chain list -> pair table -> follow-up part -> five-entry classifier-script catalog -> wrapped chain -> five-entry classifier-script catalog`, and then continues as another late repeated wrapped-chain run at `0x1609e`, `0x16249`, `0x164c9`, `0x16749`, and `0x16d23`. Those late nodes now decode with script counts `5`, `5`, `5`, `4`, and `3`, and their first classifier keys are `0x02060111`, `0x02060200`, `0x02060200`, `0x020d0201`, and `0x02040901`. A dedicated followup phase now exists in `main.c` after that late-repeat band, and the live `World.sfc` bytes are already pinned as another wrapped-chain/catalog run at `0x16f26`, `0x17108`, `0x172e8`, `0x174ca`, `0x176ac`, `0x1788d`, `0x17a72`, `0x17c56`, `0x17e38`, and `0x1801c`. A new partial wrapped-part owner reader now also exists in `main.c` for the deeper owner series after that seam: it covers the stable 27-byte `0x8008` wrapper prefix, the embedded `0x8011` part at `+0x1b`, and the later classifier-script catalog. The first owner subphase now spans `0x18204`, `0x184fe`, `0x1877f`, `0x18a00`, `0x18c81`, `0x18f02`, `0x19183`, and `0x19404` with 5-entry catalogs keyed `0x02060301`; the second subphase spans `0x19685`, `0x19a00`, `0x19d3e`, `0x1a07c`, and `0x1a3ba` with 6-entry catalogs keyed `0x02060405`. The `0x1a6f8` seam is now typed as a bounded `Scenery` series: a seed `ReadObject(new class \"Scenery\")` at `0x1a717` with an inline `sce2` gallery (`14` entries, `17` planes), followed by `113` existing-class `Scenery` bodies through `0x1da61`, grouped into `16` fixed `0x33e` blocks and phased across gallery refs `0x0201x16`, `0x0224x23`, `0x0255x8`, `0x0268x22`, `0x0297x4`, `0x02a2x17`, `0x02c7x4`, `0x02fcx8`, `0x0312x9`, and `0x032dx2`. The former raw seam at `0x1dad8` is now typed as a `part -> 289-entry classifier-script bank` corridor beginning at `0x1da9c`: the lead part is `ref=0x8008`, `state=0x2d/0x03`, `d10=0x23280000`, the bank count dword at `0x1daaf` is `0x121`, the first key is `0x02010132`, the last key is `0x02030d02`, and the bank runs through `0x2803f`. The former raw seam at `0x2803f` is now partially typed as an `Incubator` owner header plus an inline `ReadObject(new class \"Macro\")` seed and a fixed-prefix keyed script-slot run: the counted owner `CString` at `0x28049` is `Incubator`, the `Macro` class header starts at `0x28076`, the first inline macro script entry begins at `0x28089` with key `0x00000101`, and thirteen later fixed-prefix slots now pin cleanly at `0x2821d`, `0x283c1`, `0x28565`, `0x28709`, `0x288b3`, `0x28a5d`, `0x28c07`, `0x28db1`, `0x28f5d`, `0x29109`, `0x292b5`, `0x29461`, and `0x295a1`. The next local band is now partially typed too: it is a thirteen-record local script series under the shared header `0x00018333, 0, 0, <u16 key>, 0, MFC CString`, with the first four records adding a two-dword lead-in. The run begins at `0x29698` and spans starts `0x29698`, `0x299af`, `0x29cc6`, `0x29fdd`, `0x2a2f4`, `0x2a603`, `0x2a912`, `0x2aa52`, `0x2ab92`, `0x2ac87`, `0x2ad7c`, `0x2b003`, and `0x2b209`, using keys `0x0272`, `0x0272`, `0x0272`, `0x026a`, `0x026a`, `0x026a`, `0x009d`, `0x009d`, `0x0052`, `0x0052`, `0x01e2`, `0x0161`, and `0x02ec`. That local series now ends at `0x2b500`, and the sparse tail after it is partially typed too: it runs from `0x2b500` to EOF at `0x2b5c6`, begins with header dwords `0x00000180` and `0x00000179`, carries the fixed ASCII prefix `l ge 455`, then five fixed 16-byte tagged chunks with tags `0x14`, `0x24`, `0x34`, `0x44`, and `0x54`, followed by sparse footer dwords beginning `0x01110111`, `0x00000111`, `0x00000002`, `0x00000000`, `0x4f310000`, and `0x00001ffe`; the last nonzero bytes are the `cd` fill tail at `0x2b5a2..0x2b5a9`. The remaining blocker is now the higher-level owner/container above that broader post-`CallButton` segment series; this decoder-driven byte corridor is currently pinned through EOF rather than another unresolved raw seam. The strongest current owner-level candidate above that fully pinned corridor is now `FUN_004056b0`, whose load/save branches bracket `MapData`, two counted `FUN_00412d40` object loops, the macro-name catalog, the six archived child objects at `this + 0x98`, the child at `this + 0xe4`, and the bounded queued-id table at `this + 0x54` with count at `this + 0x94`. The surrounding ctor/EH thunks at `FUN_00404f40`, `FUN_00404f9b`, `FUN_00404fa9`, and `FUN_00404fcb` now also pin that owner as a vtable-backed root object with a six-element `0x0c` vector at `+0x98`, and the helper pair `FUN_00408070` / `FUN_004080a0` narrows the saved `+0x54/+0x94` tail to a bounded queued-id/event list rather than anonymous scratch state. The live-instance picture is also tighter now: `DAT_00436ea4` is the strongest current instance candidate for that root type or a very close sibling because it uses the same queued-id helper pair and the same `+0x9c/+0xe0` menu state, while `DAT_00436ebc` behaves like a separately archived child object with its own bounded `+0x80/+0x84` creature-history list that gets refreshed by `FUN_0041c130`, `FUN_0041c1b0`, and `FUN_0041c2f0`. The final `FUN_00412350()` call at the end of `FUN_004056b0` also now ties that owner more directly to creature-menu maintenance, not just passive archive I/O.
### Completed Tasks:
- `codex-science-bitmap-presenter-host-band`: Recovered the full Science Kit bitmap-presenter host seam above the recovered `+0x48` bitmap-buffer helper into `reconstruction/creatures_slice/src/science_bitmap_presenter_host.c`, covering the local file-or-blank source load, blank 8-bit bitmap creation, bounds query, client-sized fallback surface creation, palette realization, indexed-DIB rebuild, centered layout, linked-window resize propagation, and clipped blit path around `FUN_0040f220`, `FUN_0040f410`, `FUN_0040f550`, `FUN_0040f9d0`, `FUN_0040fbf0`, `FUN_0040fd00`, `FUN_0040fe10`, `FUN_0040fe60`, and `FUN_00410050`. Re-verified native and Windows `science_bitmap_presenter_host`, native and Windows `science`, and native and Windows `slice`.
- `codex-health-bitmap-strip-resource-band`: Recovered the shared Health Kit bitmap-strip resource and palette-helper seam immediately under the recovered page/controller layers into `reconstruction/creatures_slice/src/health_bitmap_resource_runtime.c`, covering the reusable multi-frame strip resource around `FUN_00409640`, `FUN_004096d0`, `FUN_004096f0`, `FUN_00409797`, `FUN_004097ac`, `FUN_004097c0`, and `FUN_004099a0`, plus the adjacent palette-builder helper pair `FUN_0040b6a0` / `FUN_0040b7c0`. Extended the isolated `health_sheet` probe with archive read or write, shared-pixel slab binding, animated frame advance, and LOGPALETTE reserved-system-band coverage, then re-verified native and Windows `health_sheet`, native `--run-bitmap-resource` smoke, and native and Windows `slice`.
- `codex-creatures-startup-scenery-machinery-scene-sequence`: Expanded the visible default `creatures` startup window to reuse the already recovered scenery and machinery world-scene previews inside the timed startup rotation, on top of the earlier annotated-population, hatchery, and incubator startup sequence. The startup first-present path in `reconstruction/apps/creatures/main.c` now stages up to five richer scenes before falling back to the old debug preview, and the updated native and Windows `creatures` builds were re-staged through the interactive VM launcher with guest PowerShell confirming `creatures_reconstruction.exe` in `SessionId 2`.
- `codex-vm-interactive-gui-launch-for-preview-apps`: Moved the remaining VM preview launchers off background `Start-Process` and onto the interactive Windows desktop session by adding the shared `scripts/launch_gui_app_in_vm_interactive.sh` helper, teaching the standalone asset/body preview launchers plus the explicit `creatures --render-world-*` preview scripts to stage tiny interactive `.cmd` wrappers, and adding `SKIP_BUILD=1` across those render probes so visible-window checks stay usable even when unrelated builds are red. Re-verified shell syntax across the new helper and all patched launchers, confirmed no preview launcher still uses direct `Start-Process`, and exercised representative asset/body/world launches with guest PowerShell showing `asset_window_render_reconstruction.exe`, `body_asset_preview_reconstruction.exe`, and `creatures_reconstruction.exe` all in `SessionId 2`.
- `codex-creatures-selection-focus-band`: Recovered the still-pending Creatures-local selection or focus corridor around `FUN_004201c0`, `FUN_004203a0`, `FUN_004203d0`, `FUN_00420490`, and `FUN_0042049c` into `reconstruction/creatures_slice/src/creatures_selection_focus.c` plus the new focused header. The recovered band now covers the local tracking-mode switch, primary-focus reset, fallback-target reacquire-or-clear path, viewport-centering helper, and its register-order split entry, with slice-harness coverage that exercises direct tracking, selected-creature reset, fallback clear, fallback dispatch, and direct split-entry centering.
- `codex-launcher-window-controller-band`: Recovered the higher-level Launcher dialog/controller shell immediately above the bitmap/palette presenter around `FUN_004020f0`, `FUN_00402210`, `FUN_00402230`, `FUN_0040229d`, `FUN_004022ab`, `FUN_004022b6`, `FUN_004022c1`, `FUN_004022d6`, `FUN_004022e0`, `FUN_004022f0`, and `FUN_00402320` into `reconstruction/creatures_slice/src/launcher_window_controller_runtime.c` plus the standalone `launcher_window_controller` probe target. The recovered band now covers the fixed `0x81` dialog-base construct, the duplicated imported stage-486 pair, the embedded bitmap-presenter teardown order, the stored context cookies at `+0x60/+0x68/+0x6c`, and the template-registration helper. Verified `bash scripts/build_reconstruction_native.sh launcher_window_controller`, `build/reconstruction_native/launcher_window_controller_reconstruction`, and `bash scripts/build_reconstruction_windows.sh launcher_window_controller`; aggregate native and Windows `slice` builds remain blocked outside this band by active Breeder compile/assert failures in `breeder_add_object_page.c` and `breeder_panel_265c_controller.c`.
- `codex-launcher-window-init-draw-band`: Recovered the adjacent Launcher window-init/draw seam immediately above the controller shell around `FUN_00402390`, `FUN_004023b0`, `FUN_004024f0`, `FUN_004025a9`, `FUN_004025d0`, `FUN_004025e0`, and `FUN_0040262b` into `reconstruction/creatures_slice/src/launcher_window_draw_runtime.c` plus the standalone `launcher_window_draw` probe target. The recovered band now covers the local `2293 -> 2081` tail wrapper, the client-rect dispatch into the embedded draw slot at `+0xd4`, the secondary-context activation plus nonclient resize/show path, and the small imported-`504`/`721` on-exit registration helpers above it. Verified `bash scripts/build_reconstruction_native.sh launcher_window_draw`, `build/reconstruction_native/launcher_window_draw_reconstruction`, `bash scripts/build_reconstruction_windows.sh launcher_window_draw`, and the aggregate native/Windows `slice` plus `launcher` targets in this checkout.
- `codex-science-bitmap-buffer-runtime-band`: Recovered the narrow Science Kit bitmap-buffer runtime seam around `FUN_0040f000`, `FUN_0040f070`, `FUN_0040f090`, `FUN_0040f11b`, `FUN_0040f130`, and `FUN_0040f1e0` into `reconstruction/creatures_slice/src/science_bitmap_buffer_runtime.c` plus the standalone `science_bitmap_buffer_runtime` probe target. The recovered slice keeps the byte-accurate `0x10` surface layout, the `0x428` DIB-info archive transfer path, the owned-versus-borrowed pixel cleanup split, and the explicit owned-pixel release helper used before the presenter swaps in borrowed DIB-section bits. Re-verified native and Windows `science_bitmap_buffer_runtime`, plus native and Windows `science`.
- `codex-science-monitor-entry-vector-teardown`: Recovered the narrow Science Kit monitor-page entry-vector teardown trio `FUN_0040e99f`, `FUN_0040e9b0`, and `FUN_0040e9f7`, and split the surrounding `FUN_0040c517` / `FUN_0040c52f` teardown coverage back into the original eight-entry slot sweep plus the separate header destroy helper inside `science_monitor_page`. The standalone `science_monitor_page` probe now checks the split wrappers directly, confirms every one of the eight four-byte entry slots is reset, confirms the header clears independently, and the aggregate native and Windows `science` targets were re-verified.
- `codex-creatures-macro-runtime-tail-band`: Recovered the adjacent macro-runtime tail corridor around `FUN_00424010`, `FUN_0042406a`, `FUN_00424080`, `FUN_00424110`, and `FUN_004241f0` into the existing `creatures_macro_runtime.{h,c}` slice as the recovered destructor, local base-reset thunk, reusable execution-state reset, script normalization helper, and register-and-dispatch wrapper. Expanded the slice harness to cover empty-script fallback, `\nendm` normalization, direct reset, direct register/dispatch, and the destructor path. Re-verified both native and Windows `slice`.
- `codex-science-font-palette-helper-band`: Recovered the shared Science Kit page GDI helper corridor around `FUN_00404490`, `FUN_004044d7`, `FUN_0040ea00`, `FUN_0040eab0`, `FUN_0040ead0`, `FUN_0040eb2d`, `FUN_0040eb40`, `FUN_0040ec60`, `FUN_0040ecf0`, `FUN_0040ed10`, `FUN_0040ed6d`, `FUN_0040ed80`, `FUN_0040eeef`, `FUN_0040ef00`, and `FUN_0040ef47` into `reconstruction/creatures_slice/src/science_page_gdi_helpers.c` plus the standalone `science_page_gdi_helpers` probe target. The recovered slice covers the shared tiny-GDI cleanup base, the five-weight font profile, and the palette builder that restores the reserved `0..9` and `0xf6..0xff` system-color bands around the copied DIB palette table. The adjacent monitor-page entry-vector helper trio `FUN_0040e99f`, `FUN_0040e9b0`, and `FUN_0040e9f7` was intentionally left out because it belongs to the already recovered `science_monitor_page` local vector teardown path rather than the shared font/palette helper band.
- `codex-creatures-imported-creature-file-loader-band`: Recovered the higher-level imported-creature file/load corridor around `FUN_00405d40`, `FUN_00405e06`, `FUN_00405e18`, and `FUN_00405fbc` into the focused `creatures_creature_import` module, added slice coverage for success/non-creature/failure/prompt paths, synced the naming map plus backlog/status notes, and re-verified native and Windows reconstruction builds.
- `codex-imported-creature-picker-helper`: Recovered the deeper shared imported-creature picker helper around `FUN_0040b840`, `FUN_0040b950`, `FUN_0040ba9f`, `FUN_0040baa7`, and `FUN_0040bab9` into the `creatures_creature_import` module, replaced the old prompt callback abstraction with recovered filter/title resource resolution plus dialog-loop semantics, added harness coverage for the `0xef18` overlength retry path and post-`.` space trimming, and verified the slice with a targeted syntax build while the aggregate native build remained blocked in active Breeder-panel work.
- `codex-imported-creature-dialog-dtor-band`: Recovered the remaining imported-creature file-dialog teardown seam directly under the recovered picker helper around `FUN_0040bb14`, `FUN_0040bb2c`, and `FUN_0040bda0` inside `creatures_creature_import`, made the stack-local dialog runtime explicit, modeled the embedded `+0xb0` dialog-string cleanup plus the adjacent base destroy wrapper, added harness coverage for the recovered `embedded-string -> base` destroy order, synced the naming map plus backlog/status notes, and verified the corridor with `clang -fsyntax-only`, native `creatures` build plus `--help`, and the Windows `creatures` build.
- `codex-launcher-bitmap-palette-band`: Recovered the Launcher-local bitmap/palette presenter corridor into `reconstruction/creatures_slice/src/launcher_bitmap_palette_runtime.c` plus the isolated `reconstruction/apps/launcher_bitmap_presenter/` probe and shared-harness coverage. This pass covers `FUN_004017a0`, `FUN_00401990`, `FUN_004019c0`, `FUN_00401a60`, `FUN_00401ad0`, `FUN_00401be0`, `FUN_00401c30`, `FUN_00401e20`, `FUN_00402f40`, `FUN_00403120`, `FUN_00403160`, `FUN_00403350`, `FUN_00403450`, `FUN_00403620`, `FUN_004036b0`, `FUN_004036d0`, and `FUN_00403740`, preserving 8-bit BMP intake, fallback indexed-surface seeding, reserved-entry `LOGPALETTE` construction, cached DIB staging, centered blit layout, and linked-host resize propagation. Re-verified native and MinGW `launcher_bitmap_presenter`; MinGW `slice` is also clean with the new shared test block, while the aggregate native `launcher` and `slice` builds remain blocked outside this slice by the older `health_bitmap_display_host.c` 64-bit layout asserts.
- `codex-creatures-wrapper-reconciliation-band`: Reconciled the pending `Creatures.exe` wrapper band around `FUN_00422e94`, `FUN_00422ea0`, `FUN_00422ed0`, `FUN_00422eef`, `FUN_00423c27`, `FUN_00423cc0`, `FUN_00423d00`, `FUN_00423e80`, `FUN_00423f60`, and `FUN_00423f80` into the nested reconstruction as `creatures_body_refresh_cleanup_genome_state_split_entry`, `creatures_creature_growth_overlay.{h,c}`, and `creatures_macro_runtime.{h,c}`. Added slice-harness coverage for the body-refresh split-entry cleanup, growth-overlay allocation or teardown, macro runtime init or destroy, execute mode, request binding selection, and owner-scoped active-macro cleanup. Re-verified both native and Windows `slice`.
- `codex-breeder-root-controller-bridge-band`: Recovered the next Breeder-local root/controller bridge above the recovered `+0x238` child shell into `reconstruction/creatures_slice/src/breeder_root_controller_bridge.c`, with a standalone `breeder_root_controller_bridge` probe covering the base/root ctor-dtor ordering, startup preference restore plus short-timer arm, child refresh toggles for the recovered `+0x238` and `+0x3168` panels, subject moniker/name refresh through the control conversation, activation gating, and the local on-exit registration helper band. Re-verified native and Windows `breeder_root_controller_bridge`, plus host-side Windows `slice`; the current aggregate native `slice` failure remains outside this slice in the unrelated `health_bitmap_display_host.c` layout drift.
- `codex-creatures-startup-scene-sequence-playback`: Improved the visible default `creatures` startup window by cycling between already recovered rich scenes through the shared preview playback host instead of presenting only one composed frame. Added `creatures_asset_preview_compose_sequence(...)` in the shared preview module, updated the startup first-present path to stage annotated-population plus hatchery scenes into a single timed startup sequence with debug fallback only if both richer scenes fail, and re-verified host clang, MinGW, aggregate native and Windows `creatures`, plus `scripts/stage_and_run_creatures_reconstruction_in_vm.sh` with guest `tasklist` confirmation.
- `codex-creatures-startup-incubator-scene-sequence`: Expanded the visible default `creatures` startup window by adding the already recovered incubator scene to the timed startup rotation. The startup first-present path now stages annotated-population, hatchery, and incubator scenes, feeds every successful rich scene into the shared sequence compositor, and keeps the old debug preview only as a final fallback. Re-verified host clang, MinGW, aggregate native and Windows `creatures`, plus `scripts/stage_and_run_creatures_reconstruction_in_vm.sh` with guest `tasklist` confirmation.
- `codex-vm-interactive-gui-launch-for-creatures`: Moved the Windows VM staging path for `creatures_reconstruction.exe` onto the interactive desktop session so the rendered startup window is not mistaken for a background Session 0 launch. `scripts/stage_and_run_creatures_reconstruction_in_vm.sh` now stages a tiny interactive `.cmd` wrapper, resolves the logged-in Windows username plus a one-time scheduled-task start time, launches through an interactive scheduled task, kills stale background instances first, and supports `SKIP_BUILD=1` so the visible-window launcher remains usable even when unrelated aggregate builds are temporarily red. Re-verified shell syntax, direct interactive-session launch behavior, and `SKIP_BUILD=1` script execution with guest PowerShell confirming `creatures_reconstruction.exe` in `SessionId 2`.
- `codex-standalone-preview-app-window-playback`: Reused the already recovered preview-window playback host in the standalone `asset_window_render` and `body_asset_preview` apps so their explicit Win32 preview windows now animate when the staged preview contains multiple frames. Both app entrypoints now call `creatures_preview_window_show_playback(..., 120u)`. Re-verified native and Windows `asset_window_render` plus `body_asset_preview`, and re-launched both through their VM staging scripts with guest `tasklist` confirmation.
- `codex-creatures-asset-body-window-playback`: Reused the already recovered preview-window playback host for the explicit asset preview and body-bank preview windows in `reconstruction/apps/creatures/main.c`, so those two rendering paths now also animate when their staged previews contain multiple frames. The body preview branch was already respecting `render_frame_count`; this pass closes the remaining visible-output gap by routing both `Creatures Asset Preview` and `Creatures Body Bank Preview` through `creatures_app_show_preview_window(...)` with the same `120 ms` cadence as the other scene windows. Host and MinGW `main.c` verification are clean when the unrelated active hatchery warning is demoted.
- `codex-creatures-scene-window-playback-integration`: Reused the already recovered preview-window playback host across the explicit world-scene preview modes so staged multi-frame world previews now animate in their own window paths instead of presenting a static scene when playback metadata is available. Added a shared `creatures_app_show_preview_window(...)` helper in `reconstruction/apps/creatures/main.c` and routed the world body, population, room layout, room label, overlay, boundary, debug, annotated, annotated-population, and world gallery windows through it with the existing `120 ms` cadence. The remaining aggregate build drift for those paths now lives outside this slice in the active Health and Launcher layout bands, not in preview-window playback itself.
- `codex-creatures-startup-annotated-scene-present`: Reused the already recovered world annotated-population preview path in the default Windows startup first-present flow so the first visible scene is now a richer world-and-creatures composition instead of the older debug-only composite when that staging succeeds. The startup path now tries the annotated-population scene with a single staged frame, keeps the shared preview-window playback host, and falls back to the existing debug preview if richer staging fails. Direct host and MinGW `main.c` compiles are green; the former full Windows `creatures` link blocker from `breeder_root_controller_bridge.c` layout drift is now cleared by the recovered Breeder root/controller bridge band.
- `codex-preview-window-frame-playback`: Added timed staged-frame playback to the shared Win32 preview host so already composed atlas previews can animate in-window instead of only presenting a static sheet. `CreaturesAssetPreview` now records grid cell metadata, `creatures_preview_window_show_playback(...)` advances staged frames on a Win32 timer, and the default startup first-present path now uses the animated preview host with a `120 ms` cadence. Verified direct host and MinGW translation-unit compiles for `creatures_preview_window_host.c` and `creatures/main.c`, a fresh Windows `creatures` aggregate build, and a VM launch check via `tasklist` showing `creatures_reconstruction.exe` running after staging.
- `codex-creatures-startup-first-present-window`: Reused the already recovered preview-window host and world-debug composite pipeline in the default Windows `creatures` launch path so the compiled binary now stages an actual first-present scene without needing a dedicated render flag. The higher-level app now accepts `--no-startup-window` as a headless opt-out, the world-debug render branch shares the same staging helper as the new startup path, and `scripts/stage_and_run_creatures_reconstruction_in_vm.sh` now launches detached through `Start-Process` so the GUI path does not hang the host command while the preview window is open. Local app translation-unit verification is green on both host clang and the MinGW cross compiler; the temporary Owner imported-stage drift that once blocked aggregate `slice` refresh has since been reconciled.
- `codex-creatures-hatchery-scene-preview`: Added the next higher-level hatchery-style asset composition above the recovered `World.sfc` header parser by staging the live world background and overlaying shipped hatchery or incubator scenery sprites `htch.spr`, `eggs.spr`, and `tub1.spr` through `tub4.spr` in the higher-level `creatures` app via `--render-world-hatchery-window`. The new helper lives in `reconstruction/engine/src/creatures_hatchery_scene_preview.c`, the VM launcher is `scripts/stage_and_run_creatures_world_hatchery_preview_in_vm.sh`, and the new path is translation-unit verified on both host clang and MinGW. Aggregate native and Windows `creatures` links are currently blocked outside this slice by active `health_bitmap_display_host.c` and `launcher_bitmap_palette_runtime.c` drift.
- `codex-creatures-incubator-scene-preview`: Added the next higher-level incubator-style asset composition above the recovered `World.sfc` header parser by staging the live world background and overlaying shipped scenery sprites `incu.spr`, `eggs.spr`, `lift.spr`, `tub2.spr`, and `tub3.spr` in the higher-level `creatures` app via `--render-world-incubator-window`. The new helper lives in `reconstruction/engine/src/creatures_incubator_scene_preview.c`, the VM launcher is `scripts/stage_and_run_creatures_world_incubator_preview_in_vm.sh`, and the new path is translation-unit verified on both host clang and MinGW. Aggregate native and Windows `creatures` links are currently blocked outside this slice by active `health_bitmap_display_host.c` and `launcher_bitmap_palette_runtime.c` drift.
- `codex-creatures-scenery-scene-preview`: Added the next higher-level scenery-gallery asset composition above the recovered `World.sfc` header parser by staging the live world background and overlaying shipped scenery sprites `sce1.spr` through `sce7.spr` in the higher-level `creatures` app via `--render-world-scenery-window`. The new helper lives in `reconstruction/engine/src/creatures_scenery_scene_preview.c`, the VM launcher is `scripts/stage_and_run_creatures_world_scenery_preview_in_vm.sh`, and the new path is translation-unit verified on both host clang and MinGW. Aggregate native and Windows `creatures` links are currently blocked outside this slice by active `health_bitmap_display_host.c` and `launcher_bitmap_palette_runtime.c` drift.
- `codex-creatures-machinery-scene-preview`: Added the next higher-level machinery-gallery asset composition above the recovered `World.sfc` header parser by staging the live world background and overlaying shipped machinery sprites `comp.spr`, `clok.spr`, `disp.spr`, `cart.spr`, `cutr.spr`, and `flag.spr` in the higher-level `creatures` app via `--render-world-machinery-window`. The new helper lives in `reconstruction/engine/src/creatures_machinery_scene_preview.c`, the VM launcher is `scripts/stage_and_run_creatures_world_machinery_preview_in_vm.sh`, and the new path is translation-unit verified on both host clang and MinGW. Aggregate native and Windows `creatures` links are currently blocked outside this slice by active `health_bitmap_display_host.c` and `launcher_bitmap_palette_runtime.c` drift.
- `codex-creatures-ambient-scene-preview`: Added the next higher-level ambient world-scene composition above the recovered `World.sfc` header parser by staging the live world background and overlaying shipped ambient sprites `boat.spr`, `fish.spr`, `bubb.spr`, `fall.spr`, `food.spr`, and `bees.spr` in the higher-level `creatures` app via `--render-world-ambient-window`. The new helper lives in `reconstruction/engine/src/creatures_ambient_scene_preview.c`, the path is verified with host clang and MinGW translation-unit compiles, aggregate Windows `creatures` plus native and Windows `slice` rebuilds, and a native staged run from a fresh host-clang relink that writes `build/reconstruction_native/creatures_world_ambient_preview.ppm`.
- `codex-creatures-world-annotated-preview`: Added the next higher-level dedicated world preview above the recovered `World.sfc` header parser by composing the existing room-bounds and boundary overlays together with the recovered room-label overlay in the higher-level `creatures` app. Wired `--render-world-annotated-window`, added `scripts/stage_and_run_creatures_world_annotated_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `bounds=(-1815,234)..(8458,1150)`, `boundary_range=587..929`, and `image=576x450`.
- `codex-creatures-world-annotated-population-preview`: Added the next higher-level dedicated world preview above the recovered `World.sfc` header parser by composing the existing room-bounds, boundary, and room-label overlays together with the recovered staged multi-creature population preview in the higher-level `creatures` app. Wired `--render-world-annotated-population-window`, added `scripts/stage_and_run_creatures_world_annotated_population_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `bounds=(-1815,234)..(8458,1150)`, `boundary_range=587..929`, `body0=dad1.gen@(24,394)`, `body1=mum1.gen@(208,386)`, `body2=dad2.gen@(392,394)`, and `image=576x450`.
- `codex-creatures-world-room-label-overlay-preview`: Added the next higher-level world preview above the recovered `World.sfc` header parser by rendering room indices and compact anchor markers directly over the staged live background asset in the higher-level `creatures` app. Wired `--render-world-room-label-window`, added `scripts/stage_and_run_creatures_world_room_label_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `bounds=(-1815,234)..(8458,1150)`, and `image=576x450`.
- `codex-creatures-world-population-preview`: Added the next higher-level asset composite preview above the recovered `World.sfc` header parser and packed body-bank pipeline by compositing several staged creature body-bank frames over the staged live world background in the higher-level `creatures` app. Wired `--render-world-population-window`, added `scripts/stage_and_run_creatures_world_population_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `body0=dad1.gen@(24,394)`, `body1=mum1.gen@(208,386)`, `body2=dad2.gen@(392,394)`, and `image=576x450`.
- `codex-creatures-world-overlay-preview`: Extended `creatures_world_room_layout_preview` with an overlay path that draws the recovered `World.sfc` room table directly over the staged live background mosaic, wired the higher-level `creatures` app with `--render-world-overlay-window`, and added `scripts/stage_and_run_creatures_world_overlay_preview_in_vm.sh`. Re-verified native and Windows `creatures`, the native `--render-world-overlay-window` run, and the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, and `room_extents=(-1815,234)..(8458,1150)`.
- `codex-creatures-world-boundary-overlay-preview`: Extended the recovered `World.sfc` header parser so it now decodes the archived `MapData` scanline-boundary table instead of skipping it, added `creatures_world_boundary_overlay_preview_apply(...)` above the active embedded-object seam, wired the higher-level `creatures` app with `--render-world-boundary-window`, and added `scripts/stage_and_run_creatures_world_boundary_preview_in_vm.sh`. Re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `room_extents=(-1815,234)..(8458,1150)`, and `boundary_range=587..929`.
- `codex-creatures-world-debug-composite-preview`: Added the next higher-level world debug preview above the recovered `World.sfc` header parser and landed world preview modes by compositing the archived `MapData` room bounds and scanline-boundary table over the staged live background asset preview. Wired the higher-level `creatures` app with `--render-world-debug-window`, added `scripts/stage_and_run_creatures_world_debug_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `room_extents=(-1815,234)..(8458,1150)`, and `boundary_range=587..929`.
- `codex-health-bitmap-display-host-band`: Recovered the distinct Health Kit indexed-bitmap display-host seam into `reconstruction/creatures_slice/include/health_bitmap_display_host.h`, `reconstruction/creatures_slice/src/health_bitmap_display_host.c`, and the `health_sheet` probe harness. Covered entries are `FUN_00402110`, `FUN_004022f0`, `FUN_00402330`, `FUN_00402520`, `FUN_00402620`, `FUN_00402660`, `FUN_00402690`, `FUN_00408740`, `FUN_00408930`, `FUN_00408960`, `FUN_004089e9`, `FUN_00408a00`, `FUN_00408a70`, `FUN_00408b80`, `FUN_00408bd0`, `FUN_00408d96`, `FUN_00408dc0`, and `FUN_00408f12`; the nearby `FUN_004088c0` remained intentionally mapped to the already recovered chemistry-gauge base cleanup. Re-verified native `health_sheet_reconstruction`, the `--run-bitmap-display-host` smoke path, and the Windows `health_sheet_reconstruction.exe` build.
- `codex-creatures-world-body-composite-preview`: Added the next higher-level asset composite preview above the recovered `World.sfc` header parser and packed body-bank pipeline by compositing a staged creature body-bank frame over the staged live world background in the higher-level `creatures` app. Wired `--render-world-body-window`, added `scripts/stage_and_run_creatures_world_body_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `gallery=Back`, `sprite=back.spr`, `frames=12`, `body_genome=dad1.gen`, `body_frames=1`, `body_image=160x32`, and `composite=(208,394)`.
- `codex-creatures-world-room-layout-preview`: Added a focused `creatures_world_room_layout_preview` engine module above the active embedded-object seam, extended `creatures_world_source_parse_header` so it now decodes the archived `MapData` room table into runtime-style bounds, wired the higher-level `creatures` app with `--render-world-layout-window`, and added `scripts/stage_and_run_creatures_world_layout_preview_in_vm.sh`. Re-verified native and Windows `creatures`, the native `--render-world-layout-window` run, and the guest launch via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The current staged live world resolves to `room_extents=(-1815,234)..(8458,1150)` across `26` rooms.
- `codex-creatures-world-gallery-preview`: Wired a world-backed asset preview into `reconstruction/apps/creatures/main.c` as `--render-world-window`, reusing the recovered `World.sfc` header parser to resolve the staged background gallery image from `background_gallery.name` before the active embedded-object seam. Added `scripts/stage_and_run_creatures_world_preview_in_vm.sh`, re-verified native and Windows `creatures`, and confirmed the live staged world currently resolves to `gallery=Back`, `rooms=26`, `entries=464`, `planes=1`, `sprite=back.spr`, with guest verification via `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`.
- `codex-breeder-registry-helper-band`: Extended `reconstruction/creatures_slice/src/breeder_bitmap_registry_runtime.c` with the adjacent Breeder-local HKCU/HKLM helper wrappers `FUN_004047a0`, `FUN_00404a30`, `FUN_00404a90`, `FUN_00404ac0`, `FUN_00404af0`, `FUN_00404b60`, `FUN_00404ba0`, `FUN_00404bf0`, `FUN_00404c40`, and `FUN_00404c80`, normalized onto the shared `CreaturesKitRegistryHandler` read/write/default helpers and expanded the existing `breeder_bitmap_registry_runtime` probe with an in-memory registry backend covering open, arbitrary read, fixed-size reads, default writes, and explicit string/dword/binary writes. Re-verified native and Windows `breeder_bitmap_registry_runtime`.
- `codex-health-panel-10f8-support-object-band`: Recovered the lower Health Kit support-object corridor around `FUN_00409e10`, `FUN_00409f70`, `FUN_0040a010`, `FUN_0040a030`, `FUN_0040a0e0`, and `FUN_0040a1c0` into `reconstruction/creatures_slice/src/health_bitmap_support_objects.c`, including the icon-loading host init helper, the generic bitmap-presenter base, the nested bottom-up 8-bit bitmap archive/blit helper, and `health_sheet` probe coverage for archive round-trips, transparent blits, and low-bit release. Re-verified native and Windows `health_sheet`, plus native and Windows `slice`.
- `codex-creatures-body-preview-integration`: Wired the already recovered live-asset body-bank preview into `reconstruction/apps/creatures/main.c` as `--render-body-window`, reusing the higher-level startup probe, staged directory resolution, and the shared preview window host instead of leaving that path isolated in `body_asset_preview`. Added `scripts/stage_and_run_creatures_body_preview_in_vm.sh`, re-verified native and Windows `creatures`, re-ran native and Windows `slice`, and confirmed the guest launch with `tasklist /FI "IMAGENAME eq creatures_reconstruction.exe"`. The remaining limitation is unchanged: this is still the packed body-bank path because the staged install tree does not contain the shipped `.qad` appendage anchor tables.
- `codex-owner-object-runtime-family`: Recovered the narrow Owner-local OLE/runtime helper band around `FUN_00402320`, `FUN_004023a0`, `FUN_004023c0`, `FUN_00402407`, `FUN_004024c0`, `FUN_00402600`, `FUN_00402620`, `FUN_00402650`, `FUN_004026c0`, `FUN_004026e0`, `FUN_00402733`, and `FUN_00402740` into `reconstruction/creatures_slice/src/owner_tool_ole_runtime.c`, with a standalone `owner_tool_ole_runtime` probe covering the runtime-host ctor/dtor wrappers, static `Owner.OLE` factory registration, on-exit shutdown registration, factory-runtime teardown, and the main-window `WM_CLOSE` shutdown bridge. Re-verified native and Windows `owner_tool_ole_runtime`, and native `slice` now passes again.
- `codex-owner-tool-controller-band`: Recovered the Owner-local top-level controller band around `FUN_00402800`, `FUN_00402820`, `FUN_00402950`, `FUN_004029ce`, `FUN_004029dc`, `FUN_004029f8`, `FUN_00402a14`, `FUN_00402a2c`, and `FUN_00402a40` into `reconstruction/creatures_slice/src/owner_tool_controller.c`, with a standalone `owner_tool_controller` probe covering the exact embedded offsets (`+0x238`, `+0x52c`, `+0x6b0`, `+0xb04`), the shell-plus-child constructor order, the reverse destroy fanout, the scalar-delete wrapper, and the final shell init forwarder. Re-verified native and Windows `owner_tool_controller`, and native `slice` still passes with this corridor included.
- `codex-owner-embedded-member-lifetimes`: Recovered the deeper Owner-local embedded-member lifetime corridor around `FUN_00404fe0`, `FUN_004055c0`, `FUN_00407a30`, `FUN_00407ff0`, and `FUN_0040bab0` into `reconstruction/creatures_slice/src/owner_tool_embedded_member_lifetimes.c`, with a standalone `owner_tool_embedded_member_lifetimes` probe covering the byte-accurate `+0x6b0`, `+0x238`, and `+0x52c` child runtimes, their scalar-delete wrappers, the shared packed page-field-group destroy helper, and the remaining local ordering around imported stages `374`, `486`, `624`, and `706`. Re-verified native and Windows `owner_tool_embedded_member_lifetimes`, plus native `slice`.
- `codex-owner-embedded-member-split-wrappers`: Recovered the remaining Owner-local split cleanup-wrapper band around `FUN_00407036`, `FUN_00407041`, `FUN_0040704c`, `FUN_00407057`, `FUN_0040706c`, `FUN_0040bc85`, `FUN_0040bc93`, `FUN_0040bca1`, `FUN_0040bcaf`, `FUN_0040bcc7`, and `FUN_0040bcdf` into `reconstruction/creatures_slice/src/owner_tool_embedded_member_split_wrappers.c`, with a standalone `owner_tool_embedded_member_split_wrappers` probe covering the hidden-`this` page-field destroy shims, the register child-host split destroy wrappers, and the direct stage `542`/`701`/`706`/`675` forwards above the recovered embedded-member lifetimes. Re-verified native and Windows `owner_tool_embedded_member_split_wrappers`, plus native `slice`.
- `codex-owner-embedded-member-unwind-thunks`: Recovered the remaining Owner-local unwind-helper band around `FUN_0040568f .. FUN_0040576b` and `FUN_004080ec .. FUN_0040820e` into `reconstruction/creatures_slice/src/owner_tool_embedded_member_unwind_thunks.c`, with a standalone `owner_tool_embedded_member_unwind_thunks` probe covering the photo-album and monitor split unwind ladders, the repeated stage `706`/`582`/`701` forwards, the repeated page-field-group destroy forwards through `FUN_00406fd0`, the photo-album bitmap-surface release at `+0x120`, and the child-host runtime release at `+0xb0`. Re-verified native and Windows `owner_tool_embedded_member_unwind_thunks`, plus native `slice`.
- `codex-owner-imported-stage-identity-reconciliation`: Resolved the remaining imported Owner runtime stages used across the recovered page/controller, embedded-member, and unwind bands into concrete semantics instead of raw ordinals. The shared runtime band now treats `486/706/762` as the MFC-backed 4-byte `CString` construct/destroy/assign stages, `439/675` as the class-cookie runtime construct/destroy pair, `374/624` as the secondary runtime-base construct/destroy pair, `509` as the shared member-shell base constructor, and `542/582/701` as the leaf/group/host member-shell destructor tails. The naming map, Owner runtime notes, and backlog rows now reflect those identities directly.
- `codex-breeder-window-session-runtime-band`: Recovered the unclaimed Breeder's Kit persisted window/session helper corridor around `FUN_004036c0`, `FUN_00403710`, `FUN_00403810`, `FUN_00403890`, `FUN_00403ae0`, `FUN_00403b70`, `FUN_00403c90`, `FUN_00403d10`, `FUN_00403d60`, and `FUN_00403d70` into `reconstruction/creatures_slice/src/breeder_window_session_runtime.c`, with a standalone `breeder_window_session_runtime` probe covering attached-font apply, registry-backed window restore and persist, startup timer arm and stop, subject-handle acquire/release, missing-subject retry prompt flow, subject-name refresh, and both local and remote quit dispatch paths. Re-verified native and Windows `breeder_window_session_runtime`; the aggregate native and Windows `slice` and `creatures` builds now pass again with the current workspace state.
- `codex-breeder-command-response-corridor`: Recovered the adjacent Breeder-local command/response corridor around `FUN_00403d90`, `FUN_00403e10`, `FUN_00403ed0`, and `FUN_00403ff0` into `reconstruction/creatures_slice/src/breeder_command_response.c`, with a standalone `breeder_command_response` probe covering command id `5` subject execution, command id `2` app-command dispatch, command id `1` response release, startup subject-refresh fallthrough, remote close dispatch, and guarded layout toggling above the recovered Breeder window/session runtime helpers. Re-verified native and Windows `breeder_command_response`, plus native and Windows `slice` and `creatures`.
- `codex-breeder-lifecycle-cleanup-band`: Recovered the adjacent Breeder-local lifecycle/cleanup family around `FUN_00401840`, `FUN_00401890`, `FUN_004018d7`, `FUN_0040428f`, `FUN_004043b0`, `FUN_00404450`, `FUN_00404470`, `FUN_004044ea`, `FUN_004044f5`, and `FUN_0040450a` into `reconstruction/creatures_slice/src/breeder_lifecycle_cleanup.c`, with a standalone `breeder_lifecycle_cleanup` probe covering the lower base-reset and middle-root cleanup helpers, the local split-entry wrappers, the derived lifecycle constructor/destroy ordering, the optional observer notify path, the paired ordinal-`706` forwarded cleanup wrappers, and the low-bit scalar-delete gate. Re-verified native and Windows `breeder_lifecycle_cleanup`, plus native and Windows `slice` and `creatures`.
- `codex-breeder-object-lifecycle-band`: Recovered the adjacent Breeder-local `+0x238` child-shell lifetime band around `FUN_0040478e`, `FUN_00404d90`, `FUN_00405220`, and `FUN_00405240` into `reconstruction/creatures_slice/src/breeder_panel_0238_shell_lifetime.c`, with a standalone `breeder_panel_0238_shell_lifetime` probe covering the split-entry base reset, constructor order for the `+0x94`, `+0x518`, `+0x6a8`, `+0x72c`, `+0x7f4`, `+0x828`, `+0xca4`, `+0xd24`, and `+0x2168` members, the ten imported-wrapper installs, the exact descending destroy chain down through the local ordinal-`706` and final ordinal-`675` stages, and the low-bit scalar-delete gate. Re-verified native and Windows `breeder_panel_0238_shell_lifetime`, plus native and Windows `slice`.
- `codex-breeder-panel-0238-member-identity-reconciliation`: Reconciled the recovered Breeder `+0x238` child-shell runtime with the already recovered `BreederPrimaryPageLifecycleState` layout: `+0x94` is now pinned as `lead_state`, `+0x0d24` as `metrics_runtime`, `+0x2168` as `bitmap_runtime`, and the grouped `+0x518`, `+0x6a8`, `+0x72c`, `+0x7f4`, `+0x828`, `+0xca4`, and `+0x21a4` bands now line up explicitly with the already recovered primary-page lifecycle state. The child-shell header and implementation now assert that identity directly, and the stale Breeder backlog rows for `0040478e .. 00405458` are synced to `verified_slice`.
- `codex-breeder-backlog-followon-reconciliation`: Reconciled the stale Breeder-local handoff notes after the `+0x238` child-shell lifecycle and member-identity bands closed. The nearby command/response, window/session, lifecycle/cleanup, and bitmap/registry notes no longer point back at `FUN_0040478e .. FUN_00405240`; after the root/controller bridge and later `+0x3168` add-object page landed, they now point forward to the remaining deeper child/controller work behind the recovered `+0x265c` root panel. The backlog summary was checked against the live exports plus naming map, and the one-count discrepancy from a naive scan is now documented as the manifest header row rather than a real uncovered function.
- `codex-breeder-panel-3168-band`: Recovered the deeper Breeder-local `+0x3168` child-panel lifecycle/controller seam around `FUN_004074c0`, `FUN_00407ae0`, `FUN_00407b00`, `FUN_00407bea .. FUN_00407cf0`, `FUN_00408140`, `FUN_00408150`, `FUN_00408170`, `FUN_004081b0`, `FUN_00408354`, `FUN_00408366`, `FUN_00408370`, `FUN_004084f1`, `FUN_00408509`, `FUN_0040851b`, `FUN_00408530`, `FUN_00408550`, `FUN_00408620`, `FUN_00408660`, `FUN_00408a50`, `FUN_00408ab0`, `FUN_00408ba0`, `FUN_00408cd0`, `FUN_00409980`, `FUN_00409a30`, `FUN_00409a50`, `FUN_0040b2a0`, `FUN_0040b340`, and `FUN_0040b360` into `reconstruction/creatures_slice/src/breeder_add_object_page.c`, with a standalone `breeder_add_object_page` probe covering the byte-accurate `0x438` page layout, support-object ctor/dtor ordering, archive-backed shop-item load/save, the fixed `Addbgd.bmp` background path, centered `Shop.bmp` preview redraw with dirty-rect union, and the `2293`/`2081` refresh-commit path. Re-verified native and Windows `breeder_add_object_page`; host-side aggregate native and Windows `slice` builds are passing again.
- `codex-body-asset-preview-window`: Added the standalone `body_asset_preview` target plus the focused `creatures_body_bank_preview` module, extending the shared `creatures_asset_preview` path so it can compose generated sprite banks from palette RGB plus in-memory sprite bytes. The new preview uses a real staged genome (`Genetics/dad1.gen` by default), parses the recovered body recipe, rebuilds the packed body bank from live `Images/*.spr` assets, writes a native `body_asset_preview.ppm`, cross-builds a Win32 preview window, and stages/launches inside the Windows 11 VM via `scripts/stage_and_run_body_asset_preview_in_vm.sh`. Re-verified native and Windows `body_asset_preview`, native and Windows `slice`, and confirmed the guest process with `Get-Process body_asset_preview_reconstruction`. The remaining body-preview gap is still the full appendage-layout path, which cannot use live assets yet because the staged install tree does not contain the shipped `.qad` anchor tables that the recovered graph-loader expects.
- `codex-preview-window-vm-smoke`: Added `scripts/stage_and_run_asset_window_render_in_vm.sh`, which cross-builds `asset_window_render`, stages `Images/` and `Palettes/` plus the exe into `~/Downloads/Creatures-97-Asset-Preview-VM`, and launches the preview window detached inside the Windows 11 guest through `powershell Start-Process`. Re-verified the host stage tree and confirmed the guest process is running with `Get-Process asset_window_render_reconstruction`.
- `codex-creatures-preview-window-integration`: Added reusable staged-asset preview helpers in `reconstruction/engine/src/creatures_asset_preview.c` and `reconstruction/engine/src/creatures_preview_window_host.c`, rewired `reconstruction/apps/asset_window_render/main.c` to use them end-to-end, and added an opt-in `--render-window [--render-frames COUNT]` preview path to `reconstruction/apps/creatures/main.c` that exits before the active embedded-object/world bootstrap. Re-verified native and Windows `asset_window_render`, plus direct object compilation of `creatures/main.c` and the new preview helpers; the full native and Windows `creatures` links are currently blocked outside this slice by active `health_add_object_page.c` layout-assert drift.
- `codex-owner-cross-page-refresh-shutdown-bridge`: Recovered the narrow Owner-local cross-page bridge around `FUN_004029ea`, `FUN_00402a06`, `FUN_00402aa0`, `FUN_00403660`, `FUN_00403750`, and `FUN_00403790` into `reconstruction/creatures_slice/src/owner_cross_page_bridge.c`, with a standalone `owner_cross_page_bridge` probe covering the inline register-record cleanup thunk, the register child-host destroy thunk, register save/shutdown persistence, the `+0x238` page plus photo-album subject-sync toggle fanout, the subject-moniker refresh bridge back through the recovered register and photo-album loaders, and the secondary-layout state bridge above the recovered shell/session helpers. Re-verified native and Windows `owner_cross_page_bridge`; the aggregate native and Windows `slice` and `creatures` links are still blocked outside this slice by active `health_add_object_page.c` layout-assert drift plus the local presenter typedef mismatch.
- `codex-owner-register-page-controller-band`: Recovered the adjacent Owner-local `"The Register"` page controller corridor around `FUN_00402af0`, `FUN_00402b50`, `FUN_00402cf0`, `FUN_00402d50`, `FUN_00402f60`, `FUN_00402f90`, `FUN_00403030`, `FUN_004031f0`, `FUN_004033a0`, and `FUN_00403440` into `reconstruction/creatures_slice/src/owner_register_page_controller.c`, with a standalone `owner_register_page` probe covering the register child-host open or close path, the `dde: getb monk` subject query, UTF-16 record-name matching, the first-slot attachment clear, the `"The Register"` archive load or save loops, blank-record append, and the short refresh-timer bootstrap. Re-verified native and Windows `owner_register_page`, plus native and Windows `slice` and `creatures`.
- `codex-asset-window-render-probe`: Added a narrow standalone `asset_window_render` preview target that loads staged `Palettes/PALETTE.DTA` plus a shipped `Images/*.spr`, binds the live on-disk `.spr` layout through the new reusable `creatures_sprite_file_view` module, composes the first twelve frames with the recovered sprite blitter, writes a native `.ppm` preview, and opens a real Win32 window on the cross-built target to present the composed asset grid without touching the active Owner register-page or embedded-object archive paths. Re-verified native `asset_window_render`, native `slice`, and Windows `asset_window_render`.
- `codex-score-window-runtime-helper-band`: Recovered the local `Score_Kit.exe` helper corridor around `FUN_00402516`, `FUN_00402520`, `FUN_00402560`, `FUN_00402590`, `FUN_004026af`, `FUN_004026c0`, and `FUN_00402707` into `reconstruction/creatures_slice/src/score_window_runtime_helpers.c`, with a standalone `score_window_runtime_helpers` probe covering the fixed `MS Sans Serif` font preset, the `kitp.WAV` sound-slot acquire/release pair at `+0xd0`, the forwarded helper-kit registry cleanup thunk, and the 32-bit nested-runtime destructor chain at `+0xc8`. Re-verified native and Windows `score_window_runtime_helpers`, plus native and Windows `slice`; the aggregate native harness currently exits with `Trace/BPT trap: 5` outside this slice.
- `codex-mfc-ordinal-4940-identity-reconciliation`: Resolved the remaining imported `MFC40.DLL` identity gap behind the body-data attachment remover using the `radare2` `mfc40.sdb.txt` export map plus the already recovered local delete loops: `4940 -> CFile::Remove`. Renamed the `FUN_00410350` reconstruction to explicit remove semantics, added naming-map coverage for the `Creatures.exe` and `remove.exe` import thunks, and re-verified native and Windows `body_data_path_forwarder`, `slice`, and `creatures`.
- `codex-body-data-path-forwarder-band`: Recovered the adjacent `FUN_00410350` body-data attachment-path helper into `reconstruction/creatures_slice/src/creatures_body_data_path_forwarder.c`, with a standalone `body_data_path_forwarder` probe covering the fixed `Body data\\<token>att` path build and the final imported remove callback dispatch. Re-verified native and Windows `body_data_path_forwarder`, plus native and Windows `slice` and `creatures`.
- `codex-science-registry-wrapper-reconciliation`: Reconciled the still-pending Science Kit helper-kit registry wrapper band around `FUN_00404950`, `FUN_00404a30`, `FUN_00404b10`, `FUN_00404b80`, and `FUN_00404c90` against the already recovered shared `creatures_kit_registry` runtime by adding `creatures_kit_registry_read_value_guarded` and `creatures_kit_registry_read_u32_from_scoped_name` to `reconstruction/engine/src/creatures_kit_registry.c` and extending the isolated `science` probe with guarded-read and scoped-name adapter coverage. Re-verified native and Windows `science`; the aggregate native `slice` rebuild is currently blocked outside this slice by a live `health_lobes_page_load_sprite_strips` static-vs-non-static declaration conflict, while the Windows `slice` target still builds.
- `codex-target-reference-gate-band`: Recovered the tiny creature target-reference gate around `FUN_00410320` and `FUN_00419090` into `reconstruction/creatures_slice/src/creatures_target_reference_gate.c`, with a standalone `target_reference_gate` probe covering the direct related-target equality helper and the current-target-or-related wrapper. Re-verified native and Windows `target_reference_gate`, plus native and Windows `slice` and `creatures`.
- `codex-temporary-imported-scope-constructor-band`: Recovered the remaining constructor/scalar-delete half around `FUN_00410420` and `FUN_00410480` directly inside `reconstruction/creatures_slice/src/creatures_temporary_scope_cleanup.c`, extending the existing `temporary_scope_cleanup` probe to cover imported-constructor call order, root vtable install, cleanup-before-delete ordering, and the low-bit scalar-delete gate for the recovered `PTR_LAB_0042fd78` temporary imported-scope family. Re-verified native and Windows `temporary_scope_cleanup`, plus native and Windows `slice` and `creatures`.
- `codex-temporary-scope-wrapper-thunks`: Recovered the wrapper-thunk band around `FUN_0041071a`, `FUN_0041072c`, `FUN_00410800`, `FUN_00410873`, and `FUN_00410888` into `reconstruction/creatures_slice/src/creatures_temporary_scope_wrapper_thunks.c`, with a standalone `temporary_scope_wrapper_thunks` probe target covering the split-entry wrapper into the `PTR_LAB_0042fe30` capture-export cleanup family, the ordinal-592 transfer-stream cleanup thunk, the direct base-vtable reset wrapper, the ordinal-706 temp-string cleanup wrapper, and the split-entry wrapper back into the lower `PTR_LAB_0042fe68` lookup-key cleanup path. Re-verified native and Windows `temporary_scope_wrapper_thunks`, plus native and Windows `slice` and `creatures`.
- `codex-temporary-lookup-key-cleanup-sibling-band`: Recovered the sibling temporary-cleanup family around `FUN_004104a0`, `FUN_004104e7`, `FUN_00410890`, `FUN_004108da`, and `FUN_004108ef` into `reconstruction/creatures_slice/src/creatures_temporary_scope_cleanup.c`, with a standalone `temporary_scope_cleanup` probe target covering the `PTR_LAB_0042fd78` imported-scope root restore, the `PTR_LAB_0042fe30` capture-export scope restore, the ordinal-706 string-cleanup forwarder, and the chained fallback into the already recovered `PTR_LAB_0042fe68` lookup-key cleanup path. Re-verified native and Windows `temporary_scope_cleanup`, plus native and Windows `slice` and `creatures`.
- `codex-temporary-lookup-key-cleanup-band`: Recovered the lower temporary lookup-key cleanup band around `FUN_00410750` and `FUN_00410797` into `reconstruction/creatures_slice/src/creatures_temporary_lookup_key_cleanup.c`, with a standalone `temporary_lookup_key_cleanup` probe target covering the outer-root vtable restore to `PTR_LAB_0042fe68` and the chained nested-object base-vtable reset through the already recovered `FUN_004014a0` helper. Re-verified native and Windows `temporary_lookup_key_cleanup`, plus native and Windows `slice` and `creatures`.
- `codex-external-tools-session-runtime-band`: Recovered the adjacent external-tool session runtime band around `FUN_0041d060`, `FUN_0041d0e0`, `FUN_0041d190`, `FUN_0041d1a0`, and `FUN_0041d1e7` into `reconstruction/creatures_slice/src/creatures_external_tools_session_runtime.c`, with a standalone `external_tools_session_runtime` probe target covering packed status broadcasts across the ten live tool sessions, the unguarded launched-tool close counter decrement, owner invalidation on close, and the temporary lookup-key cleanup thunks. Updated the shared external-tools runtime layout so `creatures_external_tools_launch_or_open(...)` now increments `active_launch_count`, and re-verified native and Windows `external_tools_session_runtime`, native and Windows `external_tools_menu`, plus native and Windows `slice` and `creatures`.
- `codex-science-monitor-page-init-band`: Recovered the remaining Science Kit `+0x238` monitor-page init corridor around `FUN_0040c7c0`, `FUN_0040d270`, and `FUN_0040d2b0` directly inside `reconstruction/creatures_slice/src/science_monitor_page.c`, and also lifted the adjacent graph-geometry helper `FUN_0040d950`. The monitor-page probe now covers the subject-toggle seed, chemical catalog rebuild, theme restore, channel-selector offset caching, five-pen creation, graph-bounds rebuild, control-selection mirroring, and one-second timer arm. Re-verified native and Windows `science_monitor_page`, native and Windows `slice`, and confirmed the shared native harness still stops outside this slice in the active pose or embedded-object archive path (`creatures_creature_pose_repeated_family_archive_read_at scripted count failed: expected 3, got 1`).
- `codex-science-monitor-page-update-band`: Recovered the remaining Science Kit `+0x238` monitor-page update and redraw corridor around `FUN_0040d320`, `FUN_0040d4c0`, `FUN_0040d6f0`, `FUN_0040d9e0`, `FUN_0040dbc0`, `FUN_0040dcd0`, `FUN_0040e2e0`, and `FUN_0040e460` directly in the focused `science_monitor_page` module, corrected the graph-left/graph-top field interpretation to the original inner-plot layout, added the recovered 256-entry scanline Y table, and extended the probe coverage for plot-surface setup, selector and theme population, redraw blits, theme-channel apply, per-channel selection sync, and DDE script publish. Re-verified native and Windows `science_monitor_page`, native and Windows `slice`, native and Windows `science`, and confirmed the shared native harness still stops outside this slice in the active pose or embedded-object archive path (`creatures_creature_pose_repeated_family_archive_read_at scripted count failed: expected 3, got 1`).
- `codex-science-monitor-page-selection-sync-helpers`: Recovered the remaining adjacent Science Kit `+0x238` monitor-page helpers `FUN_0040dac0` and `FUN_0040dc70` directly in the focused `science_monitor_page` module, covering the default or explicit plot-region clear helper with the recovered right-aligned strip clamp plus the combo-row to filtered-channel sync path that republishes the selected-channel DDE script and redraws the plot. Re-verified native and Windows `science_monitor_page`, native and Windows `science`, native and Windows `slice`, and confirmed the shared native harness still stops outside this slice in the active pose or embedded-object archive path (`creatures_creature_pose_repeated_family_archive_read_at scripted count failed: expected 3, got 1`).
- `codex-science-monitor-page-trace-and-frame-band`: Recovered the next adjacent Science Kit `+0x238` monitor-page helpers `FUN_0040dee0` and `FUN_0040dfc0` in the same focused `science_monitor_page` module, covering the four-token trace-response parser that remaps DDE samples through the recovered 256-entry scanline table plus the outer-frame, 16-pixel guide-marker, and title or bound-label draw path that shrinks the cached plot rect inward by one pixel. Extended the standalone probe with recovered frame-marker placement, label placement, and trace-response parsing coverage, re-verified native and Windows `science_monitor_page`, native and Windows `science`, native and Windows `slice`, and confirmed the shared native harness still stops outside this slice in the active pose or embedded-object archive path (`creatures_creature_pose_repeated_family_archive_read_at scripted count failed: expected 3, got 1`).
- `codex-science-monitor-page-cleanup-thunks`: Recovered the last immediately adjacent Science Kit `+0x238` monitor-page cleanup-thunk quartet `FUN_0040e267`, `FUN_0040e26f`, `FUN_0040e277`, and `FUN_0040e289` directly in the focused `science_monitor_page` module as four forwarded wrappers over the shared ordinal-`706` temporary-string cleanup routine under the recovered frame-draw path. Extended the standalone probe with direct coverage for all four forwarded cleanup calls, re-verified native and Windows `science_monitor_page`, native and Windows `slice`, and confirmed the aggregate native harness still fails outside this slice with a startup-time `Trace/BPT trap: 5`.
- `codex-science-monitor-page-cleanup-sibling-band`: Recovered the sibling local monitor-page wrapper-thunk band around `FUN_0040cb2e`, `FUN_0040cd8e`, `FUN_0040cd96`, `FUN_0040cdae`, `FUN_0040cdc0`, `FUN_0040d013`, `FUN_0040d01b`, `FUN_0040d036`, `FUN_0040d048`, `FUN_0040d4b1`, `FUN_0040d6a7`, `FUN_0040d6af`, `FUN_0040d6ca`, `FUN_0040d6dc`, `FUN_0040d913`, `FUN_0040d91b`, `FUN_0040d933`, `FUN_0040d945`, `FUN_0040daaf`, and `FUN_0040dbae` directly in the focused `science_monitor_page` module. The standalone probe now covers the repeated ordinal-706/537/592/545 forwards plus the local helper destructors that route through the shared `FUN_00402a10` and `FUN_00402c30` cleanup sequences. Re-verified native and Windows `science_monitor_page`, native and Windows `science`, and confirmed the aggregate native/windows `slice` rebuild is currently blocked outside this slice by active `health_sheet_child_shell_lifetimes.c` field-name drift.
- `codex-owner-window-session-band`: Recovered the Owner's Kit window/session corridor around `FUN_00403540`, `FUN_00403cd0`, `FUN_00403d00`, `FUN_00403d30`, `FUN_00403d60`, `FUN_00403d90`, `FUN_004084d0`, `FUN_00408570`, `FUN_004097d0`, `FUN_00409950`, `FUN_00409ba0`, `FUN_00409c30`, `FUN_00409d50`, `FUN_00409dd0`, `FUN_00409e20`, `FUN_00409e50`, `FUN_00409ed0`, and `FUN_00409f90` into `reconstruction/creatures_slice/src/owner_window_session.c`, with a standalone `owner_window_session` probe covering the local OLE automation invoke wrappers, owner-subject DDE gate, command send/release helpers, and the timer-backed startup/persist window-session path. Re-verified native and Windows `owner_window_session`.
- `codex-owner-registry-helper-band`: Closed the adjacent Owner's Kit registry helper corridor by proving `FUN_00408fe0`, `FUN_00409040`, `FUN_00409070`, `FUN_004090a0`, `FUN_004090e0`, `FUN_00409130`, `FUN_00409180`, and `FUN_004091c0` are the Owner copy of the already recovered shared helper-kit registry layer in `reconstruction/engine/src/creatures_kit_registry.c`. Added the missing shared fixed eight-byte read helper there, mapped the Owner exports plus the matching pending Science `FUN_00404ae0` wrapper onto the shared registry runtime, and re-verified native and Windows `owner_window_session` plus a standalone compile of the shared registry source; attempted native and Windows `science` builds are still blocked outside this slice by active `science_monitor_page.c` layout drift.
- `codex-owner-window-shell-band`: Recovered the Owner's Kit window-shell/controller corridor around `FUN_00409200`, `FUN_004092b0`, `FUN_004092d0`, `FUN_0040932d`, `FUN_00409340`, `FUN_00409490`, `FUN_004094b0`, `FUN_004094f1`, `FUN_00409500`, `FUN_00409600`, `FUN_0040966c`, `FUN_0040967a`, `FUN_00409688`, `FUN_00409696`, `FUN_004096ae`, `FUN_004096c0`, `FUN_00409720`, `FUN_00409780`, `FUN_004098d0`, and `FUN_00409e30` into `reconstruction/creatures_slice/src/owner_window_shell.c`, with a standalone `owner_window_shell` probe covering font-weight setup, managed-font create/release, shell construct/init/teardown, child-host attach, host-icon sync, local-vs-remote quit dispatch, and the final secondary-layout mode bit helper. Re-verified native and Windows `owner_window_shell`.
- `codex-owner-child-host-bridge-band`: Recovered the deeper Owner support-object corridor around `FUN_00403900`, `FUN_00403a00`, `FUN_00403a20`, `FUN_00403e90`, `FUN_00403f30`, `FUN_00403f50`, `FUN_00404840`, `FUN_004049ec`, `FUN_00404a8e`, `FUN_00404d76`, `FUN_00404ec0`, `FUN_00408c10`, `FUN_00408c80`, `FUN_00408ca0`, and `FUN_00408d50` into `reconstruction/creatures_slice/src/owner_window_support_objects.c`, with a standalone `owner_window_support_objects` probe covering child-host bridge/runtime init-destroy order, font-runtime cleanup bands, and registry-bridge open/destroy sequencing. Re-verified native and Windows `owner_window_support_objects`.
- `codex-owner-page-object-lifetime-band`: Recovered the first broader Owner page/object lifetime corridor around `FUN_00406ad0`, `FUN_00406b30`, `FUN_00406c20`, `FUN_00406c70`, `FUN_00406d70`, `FUN_00407130`, `FUN_00407200`, `FUN_00407220`, `FUN_004073d0`, `FUN_00407490`, `FUN_004074b0`, `FUN_00407630`, `FUN_004076d0`, and `FUN_004076f0` into `reconstruction/creatures_slice/src/owner_page_object_lifetime.c`, with a standalone `owner_page_object_lifetime` probe covering the four-slot page-field grid constructor, `"The Register"` record payload/format/destroy order, and the base plus derived photo-album record lifetime phases. Re-verified native and Windows `owner_page_object_lifetime`.
- `codex-owner-register-photoalbum-page-band`: Recovered the next Owner-local `"Photo Album"` page/controller corridor around `FUN_00405fa0`, `FUN_004060e0`, `FUN_00406120`, `FUN_00406190`, `FUN_00406460`, `FUN_004065b0`, and `FUN_004065c0` into `reconstruction/creatures_slice/src/owner_photo_album_page.c`, with a standalone `owner_photo_album_page` probe covering record append, fixed preview-rect render and present, subject-command execution, accepted delete compaction, last-record preview clear, and the `Blank.bmp` helper wrappers. Re-verified native and Windows `owner_photo_album_page`.
- `codex-owner-photoalbum-loader-band`: Recovered the next Owner-local `"Photo Album"` loader or persist corridor around `FUN_00405a90`, `FUN_00405ac0`, `FUN_00405b10`, `FUN_00405b40`, `FUN_00405b50`, and `FUN_00405d50`, and folded in the adjacent local helpers `FUN_00405f40` and `FUN_00405f60`, into `reconstruction/creatures_slice/src/owner_photo_album_loader.c`, with a standalone `owner_photo_album_loader` probe covering registry-backed selected-photo persistence under the `"Photo"` value, the `dde: getb monk` moniker lookup, `"<monk>.Photo Album"` archive load and save loops, delete-lock toggling, and save-UI preview release. Re-verified native and Windows `owner_photo_album_loader`, plus native `slice`.
- `codex-owner-photoalbum-cleanup-thunks`: Recovered the tiny remaining Owner-local photo-album cleanup-thunk band around `FUN_00405d1a`, `FUN_00405d2c`, `FUN_00405d34`, `FUN_00405f01`, `FUN_00405f19`, and `FUN_00405f2b` into `reconstruction/creatures_slice/src/owner_photo_album_cleanup_thunks.c`, with a standalone `owner_photo_album_cleanup_thunks` probe covering the paired imported-`537` forwards, the paired ordinal-`592` transfer-stream cleanup forwards, the local split-entry cleanup wrapper back into the recovered `FUN_004019d0` family, and the save-path false-return unwind helper. Re-verified native and Windows `owner_photo_album_cleanup_thunks`, plus native `slice`.
- `codex-owner-temporary-cleanup-family`: Recovered the remaining Owner-local nested temporary-cleanup family around `FUN_00401840`, `FUN_00401890`, `FUN_004018d7`, `FUN_0040193f`, `FUN_004019c7`, `FUN_004019d0`, `FUN_00401a1a`, `FUN_00401a2f`, `FUN_00401556`, `FUN_00401ee5`, `FUN_00402f2b`, and `FUN_0040337f` into `reconstruction/creatures_slice/src/owner_temporary_cleanup_family.c`, with a standalone `owner_temporary_cleanup_family` probe covering the recovered base reset helper, the nested middle and outer cleanup roots, the shared ordinal-`706` cleanup forwarder, and all local split-entry wrappers that feed back into that family from the register/photo-album cleanup paths. Re-verified native and Windows `owner_temporary_cleanup_family`.
- `codex-owner-bitmap-surface-runtime-band`: Recovered the still-unclaimed Owner-local bitmap-surface runtime around `FUN_004010b0`, `FUN_00401120`, `FUN_00401140`, `FUN_004011ae`, `FUN_004011c0`, `FUN_00401370`, `FUN_00401568`, `FUN_004016c0`, `FUN_004016f0`, `FUN_00401af0`, `FUN_00401b60`, `FUN_00401b80`, `FUN_00401c0b`, `FUN_00401c20`, `FUN_00401cd0`, `FUN_00401d10`, `FUN_00401ef7`, `FUN_00401f00`, `FUN_00402000`, and `FUN_00402040` into `reconstruction/creatures_slice/src/owner_bitmap_surface_runtime.c`, with a standalone `owner_bitmap_surface_runtime` probe covering the small 8-bit bitmap surface constructor or destructor, archive transfer, BMP load, pixel lookup, clipped blit into the larger DIB-backed surface, the larger DIB surface constructor or destructor, archive transfer, pixel release, BMP load, palette-backed create path, rectangle helper, and the paired ordinal-`592` transfer-stream cleanup forwarders. Re-verified native and Windows `owner_bitmap_surface_runtime`.
- `codex-breeder-bitmap-surface-registry-runtime-band`: Recovered the adjacent Breeder-local bitmap-entry archive/blit wrappers plus the embedded shared-registry runtime wrappers around `FUN_00404520`, `FUN_00404600`, `FUN_00404660`, `FUN_004046d0`, and `FUN_004046f0` into `reconstruction/creatures_slice/src/breeder_bitmap_registry_runtime.c`, with a standalone `breeder_bitmap_registry_runtime` probe covering bitmap transfer ordering, old-surface teardown, blit forwarding, registry-handler empty-init/destroy, and low-bit scalar delete. Re-verified native and Windows `breeder_bitmap_registry_runtime`; the aggregate native and Windows `slice` and `creatures` builds are currently blocked outside this slice by active `health_sheet_root_lifetime.c` layout drift.
- `codex-breeder-panel-265c-band`: Recovered the full Breeder-local `+0x265c` child-panel lifecycle/controller seam into `reconstruction/creatures_slice/src/breeder_panel_265c_controller.c`, with the standalone `breeder_panel_265c_controller` probe and a typed root-bridge alias. Covered entry points now span `FUN_0040c210`, `FUN_0040c440`, `FUN_0040c460`, the local destroy-wrapper band through `FUN_0040c57e`, the conversation/init/render helpers `FUN_0040c600 .. FUN_0040c8e0`, the palette and pregnancy asset corridor `FUN_0040ca00 .. FUN_0040d3db`, and the live preview/tick/command seam `FUN_0040d3f0 .. FUN_0040d680`. Re-verified native and Windows `breeder_panel_265c_controller`, native and Windows `breeder_root_controller_bridge`, and aggregate native `slice`.
- `codex-external-tools-menu-band`: Recovered the still-unclaimed external-tools registry/menu corridor around `FUN_0041c930`, `FUN_0041c952`, `FUN_0041cae3`, `FUN_0041caf0`, `FUN_0041cb12`, `FUN_0041cd27`, `FUN_0041cd40`, `FUN_0041cdd0`, and `FUN_0041cf1d` into `reconstruction/creatures_slice/src/creatures_external_tools_menu.c`, with a standalone `external_tools_menu` probe target covering payload parsing, `NumTools` updates, duplicate suppression, Tools submenu rebuilds at `0x8086 + slot`, automation-open dispatch, relative executable launch, and the local cleanup thunks. Re-verified native and Windows `external_tools_menu`; the aggregate native and Windows `slice` plus `creatures` builds are currently blocked outside this slice by in-flight `science_gene_page_controller.c` signature drift under the active Science work.
- `codex-science-chemical-selection-page`: Recovered the Science Kit `+0x238` root child band into `reconstruction/creatures_slice/src/science_monitor_page.c`, covering the constructor/destructor shell, the local imported teardown thunks, the subject-conversation gate, tick/timer helpers, selected-theme restore, and the filtered `allchemicals.str` / `chemicals.str` catalog rebuild path. Added a dedicated `science_monitor_page` probe target and re-verified native and Windows `science_monitor_page` plus native and Windows `slice`. The later update/redraw, selection-sync, trace/frame, and local cleanup-wrapper sibling bands above this page shell are now covered too; the immediate local monitor-page corridor is closed.
- `codex-science-decision-page`: Recovered the unclaimed Science Kit `+0x7fa4` root child as `science_decision_page` in `reconstruction/creatures_slice/src/science_decision_page.c`, covering the `CDecisionPage` constructor/destructor band, the 26 fixed monitor text buffers, the 256-entry gradient LUT builder, the DDE monitor startup/response path, the tick/timer helpers, and the tiny nested GDI-helper corridor around `FUN_00402a80`, `FUN_00402b60`, and `FUN_00402c30`. Added a dedicated `decision_page` probe target and re-verified native and Windows `decision_page`, native and Windows `science`, and native and Windows `slice` plus `creatures`.
- `codex-mfc-ordinal-identity-reconciliation`: Resolved the remaining imported `MFC40.DLL` identity gap behind the timer-suspended wrappers using the `radare2` `mfc40.sdb.txt` export map: `4084 -> CWinApp::OnFileNew`, `4086 -> CWinApp::OnFileOpen`, `4090 -> CDocument::OnFileSave`, and `4091 -> CDocument::OnFileSaveAs`. Renamed the recovered wrapper API in `reconstruction/engine/src/creatures_timer_suspended_ordinals.c`, added direct naming-map coverage for the four import thunks, and re-verified native and Windows `timer_suspended_ordinals`.
- `codex-creature-pairing-token-stager`: Closed the remaining local `+0x2b70` writer gap by proving from the raw decompilation that `FUN_004224c0` stages the creature's own genome token from `+0x48` into `pending_parent_genome_token` when the recovered primary behavior byte rises above `0xa0`, clears it below `0x50`, and drives the paired cached presence flags at `+0x2ada` / `+0x2adb`. Corrected the existing `creatures_creature_update_attention_and_growth` runtime layout and shared harness coverage so this corridor is no longer treated as open.
- `codex-genome-document-reset-thunk`: Recovered the tiny genome-document reset-constructor thunk `FUN_0041b420` as `creatures_genome_document_construct_default` inside `reconstruction/creatures_slice/src/creatures_genome_document_runtime.c`, collapsed the original transient/runtime vtable writes into the portable reset-to-defaults helper, updated the genome-token composer to use the recovered constructor entry point for its stack output document, and extended the standalone `genome_document_runtime` probe to verify the returned self pointer plus constructor defaults. Re-verified native and Windows `genome_document_runtime`.
- `codex-mapdata-weather-state-writers`: Recovered the higher-level `MapData` ambient weather-state band around `FUN_004011c0`, `FUN_00401390`, `FUN_004013b0`, and `FUN_00401580` into `reconstruction/engine/src/creatures_map_data_weather_state.c`, added a standalone `mapdata_weather_state` probe covering payload-only slot copies, visible-emitter rebinding, queue fallback shifting, and the disabled-slot guard, and re-verified native and Windows `mapdata_weather_state` plus native and Windows `slice` and `creatures`.
- `codex-creature-voice-token-semantics`: Closed the remaining voice-side handoff by proving the shipped `.vce` sample-token tables are packed little-endian `.wav` basenames via `reconstruction/creatures_slice/src/creatures_voice_sample_tokens.c`, adding a standalone `voice_sample_tokens` probe target, and tightening the nearby cleanup-forwarder naming so the voice/runtime and base-reset modules now describe ordinal `706` as the shared temporary-string cleanup target and ordinal `592` as the shared transfer-stream cleanup target. Re-verified native and Windows `voice_sample_tokens`, native and Windows `voice_runtime`, native and Windows `base_reset_thunks`, plus the aggregate native `slice` build.
- `codex-creature-pairing-token-producer-writer`: Tightened the remaining `+0x2b70` handoff by proving from the recovered `f**k` CAOS dispatch and the already lifted genome-token composer that the field is a staged parent genome token rather than a generic pairing handle. The recovered drive-slot chooser runtime now names it `pending_parent_genome_token`, and the only remaining local gap is the original writer that stages that token before the handoff.
- `codex-science-gene-page-sprite-host`: Recovered the narrow Science Kit `Gene.spr` page band around `FUN_0040aa00`, `FUN_0040ad00`, `FUN_0040af90`, `FUN_0040b260`, and `FUN_0040b2f0` into `reconstruction/creatures_slice/src/science_gene_page_sprite_host.c`, preserving the outer 6-byte `Gene.spr` header, the page-owned shared-pixel buffer at `+0x1cc`, the fixed-X initial blit at `0x474`, the timer-gated six-frame animation wrap, and the page-local timer stop helper. Extended the isolated `science` probe to cover palette expansion from `palette.dta`, `Gene.spr` series load, initial render, animation tick/wrap, and timer-stop fallback. Re-verified native `science`, native CLI smoke with `--load-gene-page --render-gene-page --tick-gene-page`, Windows `science`, and native and Windows `slice`.
- `codex-science-gene-page-controller-band`: Recovered the next Science Kit `+0x8cd0` gene-page controller band around `FUN_0040a870`, `FUN_0040aad0`, `FUN_0040ac90`, `FUN_0040add0`, and `FUN_0040ae40` into `reconstruction/creatures_slice/src/science_gene_page_controller.c`, widened the byte-accurate `ScienceGenePageObject` layout in the shared sprite-host header to cover the subject-session, conversation, font, child-slot, and summary/count fields up through `+0x87c`, and extended the isolated `science` probe with a full `OnInitDialog` smoke path covering child-slot binding, dual-font install, DDE reloads, owner-token decode, genetics-directory registry read, summary/range refresh, and timer arm. Re-verified native `science`, native CLI smoke with `--init-gene-page --load-gene-page --render-gene-page --tick-gene-page`, and Windows `science`.
- `codex-science-gene-page-helper-ui-band`: Recovered the remaining local Science Kit `+0x8cd0` gene-page helper/UI band around `FUN_0040a440`, `FUN_0040a550`, `FUN_0040a6c0`, `FUN_0040a710`, and `FUN_0040ab50` into `reconstruction/creatures_slice/src/science_gene_page_ui_helpers.c`, preserving the fixed 13-entry section-title order, the overview total rebuild from buckets `0..3` and `8..12`, the shared `count * 0x6a / 0x212 / 0x2e6` range math, the section-select plus `count << 3` scroll update, and the 19 embedded child-slot offsets. Extended the isolated `science` probe with direct helper assertions and re-verified native `science`, native CLI smoke with `--init-gene-page --load-gene-page --render-gene-page --tick-gene-page`, and Windows `science`.
- `codex-health-monitor-page-band`: Recovered the isolated Health Kit monitor-page animation/update corridor around `FUN_00403420`, `FUN_00403430`, `FUN_00403480`, `FUN_004034a0`, `FUN_004034b0`, `FUN_00403910`, `FUN_00403b80`, `FUN_00403bb0`, `FUN_00403c20`, `FUN_00403cd0`, `FUN_00403d80`, `FUN_00403db0`, `FUN_00403dd0`, `FUN_00403df0`, `FUN_00403fe0`, `FUN_00404000`, `FUN_004040e0`, and `FUN_00404150` into `reconstruction/creatures_slice/src/health_monitor_page.c`, with a standalone `health_monitor_page` probe covering waveform stepping, marker redraws, parsed stat-table application, asset-selection gating, timer-driven animation dispatch, and the aggregate `slice` build integration. Re-verified native and Windows `health_monitor_page`, native probe smoke with `--step-monitor --sync-monitor`, and native and Windows `slice`.
- `codex-health-monitor-restore-helper`: Recovered the adjacent Health Kit snapshot-restore helper `FUN_00403380` directly inside `reconstruction/creatures_slice/src/health_monitor_page.c`, preserving the raw saved-surface byte copy, restored-rect publication, fixed `0x7e` fill over the prior indicator band, and clipped indicator-overlay redraw. Extended the standalone `health_monitor_page` probe with direct restore coverage plus `--restore-monitor` smoke, and re-verified native and Windows `health_monitor_page` plus native and Windows `slice`.
- `codex-health-sheet-root-lifetime-runtime`: Recovered the next higher-level Health Kit sheet base/root lifetime and timer-arm band around `FUN_00404390`, `FUN_004043b0`, `FUN_004044a0`, `FUN_0040450c`, `FUN_0040451a`, `FUN_00404528`, `FUN_00404536`, `FUN_0040454e`, `FUN_00404560`, `FUN_004045c0`, `FUN_00404600`, `FUN_00407460`, `FUN_00407480`, `FUN_004074d3`, `FUN_00407600`, `FUN_00407620`, `FUN_00407661`, `FUN_00407670`, `FUN_00407770`, `FUN_004077dc`, `FUN_004077ea`, `FUN_004077f8`, `FUN_0040781e`, `FUN_00407830`, and `FUN_00407ac0` into `reconstruction/creatures_slice/src/health_sheet_base_lifetime.c` and `reconstruction/creatures_slice/src/health_sheet_root_lifetime.c`, with a standalone `health_sheet` probe covering base ctor/dtor ordering, imported wrapper teardown, root child-slot ctor/dtor order, fixed hosted-window/icon init, `(256,128)` placement fallback with desktop clamping, and the paired 90 ms plus 10 ms timer arms. Re-verified native and Windows `health_sheet`, native probe smoke with `--init-health-sheet`, and native and Windows `slice`.
- `codex-health-child-shell-lifetime-bands`: Recovered the four opaque Health root child-shell ctor/dtor bands around `FUN_004010d0` / `FUN_004012d0` / `FUN_004012f0`, `FUN_004027e0` / `FUN_00402950` / `FUN_00402970`, `FUN_004057f0` / `FUN_00405e10` / `FUN_00405e30`, and `FUN_0040a2b0` / `FUN_0040a620` / `FUN_0040a640` into `reconstruction/creatures_slice/src/health_sheet_child_shell_lifetimes.c`, narrowed the root layout in `health_sheet_root_lifetime.h` to the recovered child-shell types, and extended the standalone `health_sheet` probe with ctor/dtor coverage for the `+0x238`, `+0x10f8`, `+0x2b78`, and `+0x3eb4` shells, including imported-wrapper installs, fixed runtime seeds, `Ordinal_5439(0,4)`, and the repeated `Ordinal_762` token calls. Re-verified native and Windows `health_sheet`, native probe smoke with `--init-health-sheet --construct-child-shells`, and native and Windows `slice`.
- `codex-health-panel-3eb4-controller-band`: Recovered the first deeper Health Kit `+0x3eb4` `CAddObjectPage` controller corridor around `FUN_004060f0`, `FUN_00406170`, `FUN_00406470`, `FUN_00406480`, `FUN_004064a0`, `FUN_004064c0`, `FUN_004064e0`, `FUN_00406684`, `FUN_00406696`, `FUN_004066a0`, `FUN_00406821`, `FUN_00406839`, `FUN_0040684b`, `FUN_00406860`, `FUN_00406880`, and `FUN_00406950` into `reconstruction/creatures_slice/src/health_add_object_page.c`, modeled the byte-accurate page layout with cookie-backed item and bitmap references, and extended the standalone `health_sheet` probe with direct controller coverage plus `--run-add-object-page` smoke for the recovered `Addbgd.bmp` and `Shop.bmp` item/load path. Re-verified native and Windows `health_sheet`, native probe smoke with `--run-add-object-page`, and native and Windows `slice`.
- `codex-health-panel-3eb4-presenter-helper-band`: Recovered the remaining Health Kit `+0x3eb4` presenter/helper corridor around `FUN_00405f1a`, `FUN_00405f28`, `FUN_00405f36`, `FUN_00405f44`, `FUN_00405f52`, `FUN_00405f60`, `FUN_00405f6e`, `FUN_00405f7c`, `FUN_00405f8a`, `FUN_00405f98`, `FUN_00405fa6`, `FUN_00405fb4`, `FUN_00405fc2`, `FUN_00405fd0`, `FUN_00405fde`, `FUN_00405fec`, `FUN_00405ffa`, `FUN_00406008`, `FUN_00406020`, `FUN_00406990`, `FUN_00406fb0`, `FUN_004070e0`, `FUN_00407146`, `FUN_00407151`, `FUN_0040715c`, `FUN_00407167`, `FUN_0040717c`, `FUN_00407190`, `FUN_004071c0`, `FUN_004071f0`, `FUN_00407220`, `FUN_00407250`, `FUN_00407ec0`, `FUN_00407f40`, `FUN_00407fc0`, `FUN_00408040`, and `FUN_00408100` into the focused `health_add_object_page_presenter` and `health_subject_session` modules. The standalone `health_sheet` probe now covers preview redraw and dirty-rect union, the local background/preview/text-control cleanup wrappers, and the five-method subject automation dispatch plus the higher-level reopen/execute/send/release session helpers. Re-verified native and Windows `health_sheet`, native probe smoke with `--run-add-object-page --run-add-object-presenter --run-health-subject-session`, and native and Windows `slice`.
- `codex-health-panel-10f8-controller-band`: Recovered the deeper Health Kit `+0x10f8` chemistry-page/controller corridor around `FUN_0040a8b0`, `FUN_0040a9a0`, `FUN_0040aa30`, `FUN_0040aa80`, `FUN_0040ab50`, `FUN_0040ab90`, `FUN_0040aca0`, `FUN_0040af04`, `FUN_0040af1c`, `FUN_0040af24`, `FUN_0040af36`, `FUN_0040af50`, `FUN_0040af80`, `FUN_0040afb0`, `FUN_0040b135`, `FUN_0040b13d`, `FUN_0040b145`, `FUN_0040b14d`, `FUN_0040b15f`, `FUN_0040b170`, `FUN_0040b1b0`, `FUN_0040b250`, `FUN_0040b440`, `FUN_0040b4f0`, `FUN_0040b510`, and `FUN_0040b580` into `reconstruction/creatures_slice/src/health_chemistry_page.c`, with `health_sheet` probe coverage for the 16-chemical DDE snapshot script, `palette.dta` RGB expansion, five-child background fanout, fixed selected-chemical seed list, timer arm/stop, and the managed-font helper plus local cleanup-wrapper family. Re-verified native and Windows `health_sheet`, native probe smoke with `--run-chemistry-page`, combined native smoke with `--run-add-object-page --run-add-object-presenter --run-health-subject-session --run-chemistry-page`, and native and Windows `slice`.
- `codex-health-registry-helper-band`: Recovered the Health Kit `+0x94` registry helper corridor around `FUN_00404a90`, `FUN_00404b00`, `FUN_00404b20`, `FUN_00404bbe`, `FUN_00404bd0`, `FUN_00404e60`, `FUN_00404ec0`, `FUN_00404ef0`, `FUN_00404f20`, `FUN_00404f90`, `FUN_00404fd0`, `FUN_00405020`, `FUN_00405070`, and `FUN_004050b0` into `reconstruction/creatures_slice/src/health_registry_helper.c`, replacing the remaining opaque base-sheet registry slot with the recovered HKCU/HKLM open/close lifecycle plus the defaulted string, dword, and fixed eight-byte read/write helpers. Extended the standalone `health_sheet` probe with `--run-registry-helper` coverage, re-verified native and Windows `health_sheet`, native probe smoke with `--run-registry-helper`, and native and Windows `slice`.
- `codex-health-root-activation-fanout-band`: Recovered the remaining top-level Health Kit activation/visibility seam around `FUN_0040484d`, `FUN_00404855`, `FUN_0040485d`, `FUN_0040486f`, `FUN_00404905`, `FUN_00404917`, `FUN_00404920`, `FUN_00404960`, `FUN_004049b0`, `FUN_00407f90`, and `FUN_00407fa0` into `reconstruction/creatures_slice/src/health_root_controller_fanout.c`, and took the tiny monitor lock-toggle helper `FUN_00402e50` with it so the recovered root fanout stays exact. The `health_sheet` probe now covers the resolved root primary/secondary view flags, monitor lock shadow at `+0xe7c`, child-page toggle fanout, the activation show/hide gate, the six ordinal-706 wrappers, and the new `--run-root-fanout` smoke path. Re-verified native and Windows `health_sheet`, native probe smoke with `--run-root-fanout`, and native and Windows `slice`.
- `codex-health-shell-message-bridge-band`: Recovered the originally claimed Health-local follow-on around `FUN_004051c0`, `FUN_00405230`, `FUN_00405250`, `FUN_00405297`, `FUN_004053e0`, `FUN_00405440`, `FUN_00405450`, `FUN_00405585`, `FUN_0040559d`, `FUN_004055af`, `FUN_004055c0`, and `FUN_004055f0`, plus the adjacent base corridor at `FUN_00408600`, `FUN_004086c0`, `FUN_00408715`, `FUN_0040872a`, and `FUN_004088c0`, into `reconstruction/creatures_slice/src/health_chemistry_gauge_presenter.c`. The raw band resolved not to a top-level shell/message bridge but to the concrete `+0x10f8` chemistry-page child gauge presenter and its base resource host: the recovered `0x484` child layout now backs the five-entry vector under the chemistry page, the fixed `Gauge.spr` export path is explicit, the value-to-frame-offset redraw path is lifted, and the `health_sheet` probe now covers construct/destruct, owned-resource release, export, reset, value updates, and the new `--run-chemistry-gauge` smoke path. Re-verified native and Windows `health_sheet`, native probe smoke with `--run-chemistry-gauge`, and native and Windows `slice`.
- `codex-health-panel-2b78-controller-band`: Recovered the deeper Health Kit `+0x2b78` lobe-page/controller corridor around `FUN_004013d0`, `FUN_00401590`, `FUN_00401670`, `FUN_004016b0`, `FUN_004017b0`, `FUN_0040197f`, `FUN_00401997`, `FUN_004019a9`, `FUN_004019c0`, `FUN_00401a20`, `FUN_00401a80`, `FUN_00401a90`, `FUN_00401ab0`, `FUN_00401bb0`, `FUN_00401c30`, `FUN_00401c60`, `FUN_00401e10`, `FUN_00401e57`, `FUN_00401e60`, `FUN_00401eef`, `FUN_00401f62`, `FUN_00401f77`, `FUN_00401f80`, `FUN_00401fca`, and `FUN_00401fdf` into `reconstruction/creatures_slice/src/health_lobes_page.c`, covering subject-conversation open, the 1000 ms poll-interval seed, `dde: lobe,endm` layout parsing, `Lobes.spr` strip-bank load, surface snapshot capture/restore, density-grid parse, six-region frame updates, presenter invalidation, and the local cleanup wrappers. Extended the standalone `health_sheet` probe with wrapper/controller coverage and re-verified native and Windows `health_sheet`, native probe smoke with `--init-health-sheet --construct-child-shells --run-lobes-page`, and native and Windows `slice`.
- `codex-health-lobes-page-build-reconciliation`: Closed the remaining local `health_lobes_page` follow-on by reconciling the exported `health_lobes_page_load_sprite_strips` declaration/definition mismatch, recovering the adjacent `FUN_00401a60`, `FUN_00401ff0`, `FUN_00402000`, and `FUN_00402040` tail helpers as the paired tick-flag toggle, the two no-op point constructors, and the cleanup-wrapper release, and re-verifying native and Windows `health_sheet` plus native and Windows `slice`.
- `codex-creature-genome-document-helper-band`: Recovered the remaining neighboring genome-document helper band around `FUN_0041b50a` and `FUN_0041b620` into `reconstruction/creatures_slice/src/creatures_genome_document_archive.c`, with a standalone `genome_document_archive` probe covering the stack cleanup thunk, archive write mode, archive write shortfall, archive read mode, and archive read shortfall. This pass also reconciled the stale notes that still listed `FUN_0041be70` as open even though it already lives in the body-gene parser module. Re-verified native and Windows `genome_document_archive`, plus native and Windows `slice` and `creatures`.
- `codex-creature-pairing-token-producer`: Reconciled the nearby creature pairing-field split around the recovered drive-slot chooser by proving from `FUN_00424710` / `FUN_004251f0` that CAOS `baby` reads and writes the 32-bit token slot at `+0x2b74`, correcting the portable CAOS target view and narrowing the still-open producer-side question to `+0x2b70` only. The remaining writer path for `+0x2b70` is still separate lift work.
- `codex-timer-suspended-ordinal-wrappers`: Recovered the four timer-suspended ordinal wrappers around `FUN_00406ec0`, `FUN_00406ee0`, `FUN_00429d60`, and `FUN_00429d80` into `reconstruction/engine/src/creatures_timer_suspended_ordinals.c`, preserving the exact `creatures_window_timer_stop -> imported MFC ordinal -> creatures_window_timer_start` order. Added a standalone `timer_suspended_ordinals` probe target and re-verified native and Windows builds.
- `codex-creature-genome-document-runtime`: Recovered the genome-document lifecycle and query helper band around `FUN_0041b400`, `FUN_0041b4b0`, `FUN_0041b520`, `FUN_0041b603`, `FUN_0041b998`, and `FUN_0041be20` into `reconstruction/creatures_slice/src/creatures_genome_document_runtime.c`, with a standalone `genome_document_runtime` probe covering constructor defaults, token-backed genome load, loaded-buffer release, record counting, heap reserve, and store-side cleanup. Re-verified native `genome_document_runtime`, native `genome_token_composer`, native and Windows `slice`, and native and Windows `creatures`.
- `codex-creature-token-combiner`: Recovered the genome-token composition corridor around `FUN_0041b7f0`, `FUN_0041b8fb`, `FUN_0041b903`, `FUN_0041b915`, `FUN_0041b920`, `FUN_0041b9b0`, `FUN_0041baf0`, `FUN_0041bb10`, `FUN_0041bb40`, `FUN_0041bb60`, `FUN_0041bba0`, `FUN_0041bc20`, `FUN_0041bca0`, and `FUN_0041bd10` into `reconstruction/creatures_slice/src/creatures_genome_token_composer.c`, with a standalone `genome_token_composer` probe covering unique-token generation, mutable-byte copy, clone composition, and two-parent recombination. Re-verified native and Windows `genome_token_composer`, `slice`, and `creatures`.
- `codex-boot-sequence-wrappers`: Recovered the thin boot-sequence wrapper band around `FUN_00429270`, `FUN_004294bd`, `FUN_004294d5`, `FUN_004294e0`, `FUN_00429521`, `FUN_00429550`, `FUN_0042959e`, `FUN_004295b2`, and `FUN_0042983c` into `reconstruction/engine/src/creatures_boot_sequence_wrappers.c`, with a standalone `boot_sequence_wrappers` probe covering both fixed `_onexit` thunk registrations, the guarded `Ordinal_651` wrapper plus its duplicate aliases, the paired `Ordinal_721` aliases, and the final `Ordinal_549` forwarder. Re-verified native and Windows `boot_sequence_wrappers`, native `slice`, and confirmed the aggregate native harness still stops only at the unrelated startup-asset probe.
- `codex-boot-registry-privilege-band`: Recovered the boot-registry privilege and uninstall-string band around `FUN_00429360`, `FUN_00429540`, and `FUN_00429860` into `reconstruction/engine/src/creatures_boot_registry.c`, with a standalone `boot_registry` probe target covering `Burble` dword read-or-default writeback, `Privileges` string mapping to the recovered Darwin and Cheese privilege flags, the published singleton preferences object, and the HKLM uninstall-string update to `SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\CreaturesDeinstKey`. Re-verified native and Windows `boot_registry`, native `slice`, and confirmed the aggregate native harness still stops only at the unrelated startup-asset probe.
- `codex-science-serialized-band-container`: Recovered the higher-level Science Kit serialized-band container band around `FUN_0040b840`, `FUN_0040b8d0`, `FUN_0040b8f0`, `FUN_0040b997`, `FUN_0040b9ac`, `FUN_0040b9c0`, and `FUN_0040bba0` into `reconstruction/creatures_slice/src/science_serialized_band_series.c`, corrected the `+0x954` / `+0x9a4` chemical-graph page vectors to hold the recovered `0x28` series object instead of an opaque placeholder, and extended the isolated `science` probe to verify series archive read/write, shared-pixel ownership, rotating frame blits, and the page-level vector constructor/destructor callbacks. Re-verified native and Windows `science`.
- `codex-mapdata-runtime`: Recovered the `Creatures.exe` `MapData` runtime cluster around `FUN_0042d4f0`, `FUN_0042d640`, `FUN_0042d720`, `FUN_0042daa0`, `FUN_0042db10`, `FUN_0042db40`, and `FUN_0042db90` into `reconstruction/engine/src/creatures_map_data_runtime.c` with a standalone `mapdata` probe target under `reconstruction/apps/mapdata/`.
- `codex-mapdata-runtime-helpers`: Recovered the tiny `MapData` helper band around `FUN_0042d4d0`, `FUN_0042d660`, `FUN_0042d778`, and `FUN_0042d79a` into the existing `creatures_map_data_runtime` module, preserving the `"MapData"` runtime-class lookup wrapper, room-constructor identity export, the 100-slot ambient unwind walk, and the base-vtable reset thunk. Extended the standalone `mapdata` probe target to cover all four helpers and re-verified native and Windows `mapdata`.
- `codex-startup-tip-manager`: Recovered the startup-tip manager corridor around `FUN_00419330`, `FUN_004194de`, `FUN_004194e6`, `FUN_00419510`, `FUN_00419530`, `FUN_004195b2`, `FUN_004195c7`, `FUN_00419640`, `FUN_00419700`, `FUN_00429bf0`, and `FUN_00429c4f` into `reconstruction/engine/src/creatures_startup_tip_manager.c`, preserving the stack-constructed tip session, persisted `TipStartup`/`TipFilePos`/`TipTimeStamp` handling, filtered line reader, startup reset helper, and the local unwind cleanup thunks. Added a standalone `startup_tip_manager` probe target and re-verified native and Windows builds.
- `codex-creature-eye-window-lifetime`: Recovered the eye-window/view helper band around `FUN_00428cc0`, `FUN_00428ce0`, `FUN_00428d47`, `FUN_00428d50`, `FUN_00428df5`, `FUN_00428e00`, `FUN_00428eb0`, `FUN_00429040`, `FUN_00429070`, and `FUN_00429090` into `reconstruction/engine/src/creatures_eye_window.c`, with a standalone `eye_window` probe target covering window creation at persisted `EyePosn`, out-of-range saved-position reset, title refresh, teardown, scalar delete, and the display-surface palette/backbuffer forwarding wrappers.
- `codex-creature-eye-window-follow-update`: Recovered the eye-window follow-update pair `FUN_004290b0` and `FUN_004290bb` into `reconstruction/engine/src/creatures_eye_window.c`, with the standalone `eye_window` probe extended to cover snapped and eased tracking, wrapped horizontal seam crossing, invalid cached-focus fallback to live creature bounds, and redraw-scope forwarding through `creatures_display_surface_prepare_and_redraw_region`.
- `codex-body-gene-effect-runtime`: Recovered the next body-gene effect runtime consumers `FUN_0042c3d0` and `FUN_0042c500` into `reconstruction/creatures_slice/src/creatures_body_gene_effect_runtime.c`, with a standalone `body_gene_effect_runtime` probe target covering simple-layer floor-delta collection, strongest-root preservation, byte-band program evaluation, per-channel emitter contributions, selector-budget updates, and injected selector-one or selector-two dispatch. Re-verified native and Windows `body_gene_effect_runtime`; the aggregate `slice` rebuild is currently blocked outside this lift by the unrelated startup-fixture failure in the shared harness.
- `codex-body-gene-effect-selectors`: Recovered the selector-specific body-gene effect follow-ons `FUN_0042c2c0`, `FUN_0042cf10`, `FUN_0042d000`, and the shared source-signature helper `FUN_0042d2a0` into `reconstruction/creatures_slice/src/creatures_body_gene_effect_selectors.c`, with a standalone `body_gene_effect_selectors` probe target covering the recovered layer-set update ordering, base-layer activity merge, selector-one random active-source seeding, selector-two duplicate-signature rejection, and emitter restore behavior. Re-verified native and Windows `body_gene_effect_selectors`, plus the existing `body_gene_effect_runtime`; the unrelated Science Kit serialized-band layout drift that briefly blocked the aggregate `slice` rebuild has now been fixed.
- `codex-body-gene-post-selector-state`: Recovered the post-selector body-gene effect helper pair `FUN_0042d2e0` and `FUN_0042d380` into `reconstruction/creatures_slice/src/creatures_body_gene_effect_post_update.c`, with a standalone `body_gene_effect_post_update` probe target covering per-layer cell reset, emitter current-strength clear plus target-to-display sync, and the follow-on target-strength clamp that preserves already-raised display strengths. Re-verified native and Windows `body_gene_effect_post_update`, native `body_gene_effect_selectors`, and the aggregate native `slice` build; the remaining runtime failure is still the unrelated `reconstruction_probe_creatures_startup all assets failed` startup-asset probe.
- `codex-component-easing-helper`: Recovered the table-driven component-easing helper at `FUN_00428b80` and lifted its already recovered sibling `FUN_00428b30` out of the catch-all runtime file into `reconstruction/creatures_slice/src/creatures_component_easing.c`, with a standalone `component_easing` probe target covering the mask-gated fixed-point scale and the non-mutating target-blend path. Re-verified native and Windows `component_easing`, `slice`, and `creatures` app builds.
- `codex-body-gene-runtime-class-helper`: Recovered the tiny `Ordinal_4836` bridge `FUN_00429e80` into `reconstruction/creatures_slice/src/creatures_brain_runtime_class.c`, with a standalone `brain_runtime_class` probe target covering the preserved archive-context return, the literal `"CBrain"` runtime-class lookup, and the null-output-slot early return. Re-verified native and Windows `brain_runtime_class` plus the aggregate native `slice` build; the remaining runtime failure is still the unrelated `reconstruction_probe_creatures_startup all assets failed` startup-asset probe.
- `codex-body-gene-profile-readers`: Recovered the bounded-byte body-gene profile reader band around `FUN_00429ea0`, `FUN_00429ed0`, `FUN_00429f40`, `FUN_0042a170`, and `FUN_0042a1b0` into `reconstruction/creatures_slice/src/creatures_body_gene_profiles.c`, with a standalone `body_gene_profiles` probe target covering the repeated 10-byte byte-band reader, the 15-byte color patch reader, the 58-byte nested profile reader, the product-floor helper, and the top-level 176-byte variant profile reader. Re-verified native and Windows `body_gene_profiles`, `slice`, and `creatures` app builds.
- `codex-body-gene-variant-layout`: Recovered the body-gene variant-layout consumer band around `FUN_0042a2f0`, `FUN_0042ba10`, `FUN_0042bbb0`, and `FUN_0042bc20`, plus the tiny local helper trio `FUN_0042a5e0`, `FUN_0042a600`, and `FUN_0042a610`, into `reconstruction/creatures_slice/src/creatures_body_gene_variant_layout.c`, with a standalone `body_gene_variant_layout` probe target covering runtime table allocation, per-cell sample expansion, cross-variant scatter/linking, archive two-pass ordering, normalized target indices, and first-variant selector-`2` cell tagging. Re-verified native and Windows `body_gene_variant_layout`; the aggregate native `slice` build is still blocked outside this slice by unrelated active archive/pose compile failures.
- `codex-body-gene-variant-set-lifecycle`: Recovered the duplicate body-gene runtime-layout entry and the owning variant-set lifecycle band around `FUN_0042a30f`, `FUN_0042a6a0`, `FUN_0042a750`, `FUN_0042a770`, `FUN_0042a7c1`, and `FUN_0042a7e4` by extending `reconstruction/creatures_slice/src/creatures_body_gene_variant_layout.c`, with the standalone `body_gene_variant_layout` probe target extended to verify the recovered base-versus-derived vtable token transitions, per-record vtable seeding across all sixteen `0xc2` records, the duplicate runtime-layout entry, full-array destructor cleanup, and low-bit-gated scalar-delete forwarding. Re-verified native and Windows `body_gene_variant_layout`; the aggregate native `slice` build is still blocked outside this slice by unrelated active archive/harness drift.
- `codex-body-gene-sample-mutation-export`: Recovered the body-gene sample mutation/export helper band around `FUN_0042bde0`, `FUN_0042be10`, `FUN_0042be60`, `FUN_0042beb0`, `FUN_0042bef0`, `FUN_0042bfc0`, and `FUN_0042c210` into `reconstruction/creatures_slice/src/creatures_body_gene_sample_mutation_export.c`, renamed the proved cell/sample bytes in the shared variant-layout structs, and added the standalone `body_gene_sample_mutation_export` probe target covering the packed dword writer, seed/accumulated mutation helpers, absolute-position lookup, packed triplet export, and verbose cell summary formatting. Re-verified native and Windows `body_gene_sample_mutation_export`; broader `slice`/`creatures` validation remains outside this corridor because the active archive and shared-harness slices are still moving.
- `codex-body-gene-identity-helper`: Recovered the last standalone body-gene export `FUN_0042a120` into `reconstruction/creatures_slice/src/creatures_body_gene_identity_helper.c`, with a standalone `body_gene_identity_helper` probe target verifying that the recovered export preserves both ordinary tokens and the all-ones sentinel unchanged. Re-verified native and Windows `body_gene_identity_helper`; broader `slice`/`creatures` validation stays outside this cleanup slice because the active archive and Science work are still moving.
- `codex-viewport-controller-split-entry-reconciliation`: Reconciled the still-pending viewport split-entry aliases `FUN_0040364b` and `FUN_0040370b` against the already recovered `FUN_00403640` / `FUN_00403700` target-anchor helpers inside `reconstruction/engine/src/creatures_viewport_controller.c`, added explicit shared-harness coverage plus a dedicated `viewport_controller` probe target for the alias entry semantics, and updated the backlog/notes so those duplicate rows no longer remain pending. Re-verified native and Windows `viewport_controller` plus native and Windows `slice`; the remaining runtime failure is still the unrelated startup-asset probe.
- `codex-science-serialized-band-helper`: Recovered the unclaimed Science Kit helper cluster around `FUN_00403660`, `FUN_004036d0`, `FUN_004036f0`, `FUN_00403770`, `FUN_00403870`, and `FUN_004038a0` into `reconstruction/creatures_slice/src/science_serialized_band.c`, with the isolated `science` probe extended to verify the byte-accurate `0x18` band layout via a 32-bit cookie plus host-side pointer sidecar, archive read/write shortfall callbacks, owned-versus-borrowed pixel storage, bottom-up pixel addressing, opaque blit, transparent clipped blit, and the low-bit scalar-delete release wrapper. Re-verified native and Windows `science`, and the aggregate native and Windows `slice` builds now clear again.
- `codex-mapdata-weather`: Recovered the `MapData` ground-table mutation arm from `FUN_00425cd0`, the adjacent ambient-slot updater `FUN_0042dbc0`, and the tiny ambient profile helpers they require into `creatures_map_data_weather.{h,c}` with a standalone `mapdata_weather` probe target.
- `codex-creature-constructor-import`: Recovered the higher-level `Creatures.exe` creature-constructor/import plumbing around `FUN_00422aa0` and `FUN_004233c0` into `reconstruction/creatures_slice/src/creatures_creature_constructor.c` with harness coverage for the constructor sequence, balanced-sex fallback, brain/biochemistry attachment, default pose/placement, and the 14-bucket genome summary helper.
- `codex-creature-post-growth-formatters`: Recovered the adjacent `Creatures.exe` post-growth/state-signaling helpers `FUN_00422ff0`, `FUN_00423070`, and `FUN_00423340` into `reconstruction/creatures_slice/src/creatures_creature_post_growth.c` with harness coverage for attachment teardown, one-shot growth finalization, selected-eye-window cleanup, broadcast-color emission, and companion-channel identity payload publishing.
- `codex-creature-caos-export-bridge`: Recovered the next constructor-adjacent macro bridge/registry layer around `FUN_00423660`, `FUN_004236b0`, `FUN_00423965`, `FUN_00423c60`, `FUN_00424100`, `FUN_00424210`, `FUN_00424250`, `FUN_00424300`, and `FUN_004243a0` into `reconstruction/creatures_slice/src/creatures_macro_registry.c` with harness coverage for runtime-class lookup, active-macro registration/removal, deferred destroy dispatch, name-binding replacement/compaction, and full registry teardown.
- `codex-creature-macro-export-serialization`: Recovered the constructor-adjacent macro-name serialization/export layer around `FUN_004242c0`, `FUN_00424410`, `FUN_00424523`, `FUN_00424540`, and `FUN_00424620` into `reconstruction/creatures_slice/src/creatures_macro_archive.c` with harness coverage for exact-key lookup, high-24-bit filtered export counts, full-table export, the two temporary-string cleanup thunks, and the recovered buffer-overrun throw path.
- `codex-creature-macro-import-read`: Recovered the constructor-adjacent macro-name import helper `FUN_00424640` and its cleanup thunk `FUN_00424706` into the existing macro archive slice in `reconstruction/creatures_slice/src/creatures_macro_archive.c`, with harness coverage for the import-side cleanup thunk, full-table `(count, key, temporary_name)` reads into the live macro registry, and duplicate-string failure cleanup. Re-verified `bash scripts/build_reconstruction_native.sh slice`, `bash scripts/build_reconstruction_windows.sh slice`, and a standalone smoke harness for the import path.
- `codex-creature-caos-value-access`: Recovered the CAOS numeric token accessor/setter corridor around `FUN_00424710`, `FUN_004251f0`, and `FUN_004253e0` into `reconstruction/creatures_slice/src/creatures_caos_value_access.c` with harness coverage for decimal literals, indexed `var`/`obv` slots, recursive selector tokens (`room`, `totl`, `driv`, `chem`, `touc`, `tokn`, `scor`), the supported lvalue writes/clamps, and bracketed string extraction. Re-verified `bash scripts/build_reconstruction_native.sh slice`, `bash scripts/build_reconstruction_windows.sh slice`, and a standalone smoke harness for the new module; the shared native probe still stops in the broader startup asset check outside this CAOS slice.
- `codex-creature-caos-app-dispatch`: Recovered the narrow constructor-adjacent CAOS `'app:'` dispatcher body `FUN_00425f80` into `reconstruction/creatures_slice/src/creatures_caos_app_dispatch.c`, with a standalone `caos_app_dispatch` probe target covering the sole recovered `quit` arm, the preserved ignored numeric rvalue read, and the shared `'app:' command` syntax-error fallback. Re-verified native and Windows `slice` plus `creatures` app builds.
- `codex-creature-caos-output-formatter`: Recovered the constructor-adjacent CAOS `dde:` output/formatter body `FUN_00425420` into `reconstruction/creatures_slice/src/creatures_caos_output_formatter.c`, with a standalone `caos_output_formatter` probe target covering `putb`, `getb`, `word`, `gene`, `lobe`, `scrp`, `cell`, `puts`, `pict`, and `putv`, and re-verified native and Windows `slice` plus `creatures` app builds.
- `codex-creature-caos-sys-dispatch`: Recovered the mixed constructor-adjacent CAOS `'sys:'` dispatcher body `FUN_00425cd0` into `reconstruction/creatures_slice/src/creatures_caos_sys_dispatch.c`, with shared harness coverage for `wrld`, `cmra`, `grnd`, `cmnd`, `wpos`, `wtop`, `edit`, `quit`, `abrt`, and the shared `'sys:' command` syntax-error fallback. Re-verified native and Windows `slice`; the shared native harness still stops only at the unrelated startup-asset probe outside this dispatcher slice.
- `codex-creature-caos-object-commands`: Recovered the write-side CAOS object-command corridor around `FUN_00425fb0`, `FUN_004264b0`, and `FUN_004265c0` into `reconstruction/creatures_slice/src/creatures_caos_object_commands.c`, with harness coverage for the recovered `NEW:` constructor tags (`bkbd`, `crea`, `vhcl`, `cbtn`, `scen`, `comp`, `simp`, `lift`), the `gene` lvalue bridge, the `part` selected-part update, the `BBD:` `edit` / `word` / `emit` / `show` dispatchers, and the exact modal syntax-error message formatting. Re-verified `bash scripts/build_reconstruction_native.sh slice`, `bash scripts/build_reconstruction_windows.sh slice`, and the shared native harness up to the unchanged startup-asset probe outside this CAOS slice.
- `codex-creature-caos-interpreter-dispatch`: Recovered the broader constructor-adjacent CAOS interpreter layer at `FUN_00426670` into `reconstruction/creatures_slice/src/creatures_caos_interpreter_dispatch.c`, with a standalone `caos_interpreter_dispatch` probe target covering the recovered `dde:` / `sys:` / `app:` bridge routing, `gsub` / `subr` / `retn`, `reps` / `repe`, `doif` / `else` / `endi`, `enum` / `next`, `wait`, `scrp` / `scrx`, selector updates (`targ`, `part`), the arithmetic lvalue corridor (`setv`, `addv`, `subv`, `andv`, `orrv`, `mulv`, `divv`, `modv`, `rndv`, `negv`), and the shared top-level `Instruction` syntax-error path. Re-verified `bash scripts/build_reconstruction_native.sh caos_interpreter_dispatch`, `./build/reconstruction_native/caos_interpreter_dispatch_reconstruction`, `bash scripts/build_reconstruction_native.sh slice`, and `bash scripts/build_reconstruction_windows.sh caos_interpreter_dispatch`.
- `codex-kit-registry-science`: Recovered the shared helper-kit registry layer in `reconstruction/engine/src/creatures_kit_registry.c`, the `Science_Kit.exe` chemical persistence helpers in `reconstruction/creatures_slice/src/science_tool.c`, and the isolated `science` probe target under `reconstruction/apps/science/`. Covered entries include the science registration/registry cluster plus the sibling owner/health/breeder/score/funeral tool-registration wrappers.
- `codex-science-dde-commands`: Recovered the Science Kit DDE command assembly/send path around `FUN_004025c0`, `FUN_00407040`, `FUN_00407170`, and the immediate local transport helpers `FUN_00408190`, `FUN_004081c0`, and `FUN_00408300` into `reconstruction/creatures_slice/src/science_dde.c`, with the isolated `science` probe target extended to verify the monitor bootstrap script, the selected-chemical query builder, and the send/release wrapper.
- `codex-science-dosage-page`: Recovered the narrow Science Kit dosage-page controller cluster around `FUN_004069a0`, `FUN_004069b0`, `FUN_00406bd0`, `FUN_00406be0`, `FUN_00406bf0`, `FUN_00406dd0`, and `FUN_004071f0` into `reconstruction/creatures_slice/src/science_dosage_page.c`, with the isolated `science` probe target extended to verify the seven-line `injections.str` combo reload, the graph-band scroll updater, the dosage sprite-bank reload path, and the special selection-1 back-pane-first refresh ordering.
- `codex-science-subject-session`: Recovered the narrow Science Kit subject/session gate around `FUN_00404b10`, `FUN_00407de0`, `FUN_00407e70`, `FUN_00407ec0`, `FUN_00407f00`, `FUN_004080c0`, `FUN_00408140`, and `FUN_004081a0` into `reconstruction/creatures_slice/src/science_subject_session.c` plus the shared string-default registry helper in `reconstruction/engine/src/creatures_kit_registry.c`, with the isolated `science` probe target extended to verify the privilege fallback/writeback path, one missing-subject retry, owner-token hex decode, subject-name load, and conversation open/close sequencing.
- `codex-science-window-session`: Recovered the narrow Science Kit window/session lifecycle corridor around `FUN_00404c90`, `FUN_004074e0`, `FUN_00407550`, `FUN_00407900`, `FUN_00407960`, `FUN_004079c0`, `FUN_00407a10`, `FUN_00407b10`, `FUN_00407b90`, and `FUN_00408240` into `reconstruction/creatures_slice/src/science_window_session.c` plus the shared binary-default registry helper in `reconstruction/engine/src/creatures_kit_registry.c`, with the isolated `science` probe target extended to verify startup restore/persist, icon sync, trackbar-to-scroll lookup, graph timer kick, and local versus hosted quit dispatch.
- `codex-science-sheet-root-lifetime`: Recovered the first full Science Kit sheet-root constructor/destructor band around `FUN_00402e80`, `FUN_00402ea0`, `FUN_00402fb0`, and the immediate local teardown or reinit thunks `FUN_00403025` through `FUN_00403080` into `reconstruction/creatures_slice/src/science_sheet_root_lifetime.c`, with the isolated `science` probe target extended to verify the corrected `0xa08c` root object layout, the five embedded-page slot offsets and ctor/dtor order, the root-vtable overwrite, the repeated window/icon init thunk, and the scalar-delete release wrapper above the recovered sheet-base lifetime layer.
- `codex-science-sheet-runtime-controller`: Recovered the next Science Kit sheet runtime/controller band around `FUN_004030e0`, `FUN_00403420`, `FUN_00403450`, `FUN_004034c0`, and `FUN_00403510` into `reconstruction/creatures_slice/src/science_sheet_runtime_controller.c`, with the isolated `science` probe target extended to verify the recovered window-restore-plus-10ms timer arm path, the refresh and timer-stop fanout across the `+0x238`, `+0x7fa4`, `+0x8cd0`, `+0x958c`, and `+0xe10` child pages, the paired subject-state transition branches, and the corrected `+0x958c` page-tail flags at offsets `+0xaf8` and `+0xafc`.
- `codex-science-sheet-embedded-page-host`: Recovered the Science Kit base embedded page-host band at `ScienceSheetBaseObject + 0xb0`, centered on `FUN_00403aa0`, `FUN_00403ba0`, `FUN_00403bc0`, `FUN_00403c2c`, `FUN_00403c3a`, `FUN_00403c48`, `FUN_00403c56`, `FUN_00403c6e`, `FUN_00403ca0`, and `FUN_00403dc0`, into `reconstruction/creatures_slice/src/science_sheet_embedded_page_host.c`, with the isolated `science` probe target extended to verify the fixed `0x8c` dialog-base stage, the nested bridge object at `+0xa4`, the duplicate imported `486` and `706` stage pairs, the owner/context/icon init path, the guarded large-icon sync, and the low-bit scalar-delete release wrapper.
- `codex-science-chemical-page-lifetime`: Recovered the larger Science Kit page/controller band rooted at `FUN_00406130` into `reconstruction/creatures_slice/src/science_chemical_graph_page.c`, including the scalar-delete wrapper `FUN_004063c0`, destructor `FUN_004063e0`, local teardown thunks `FUN_00406470` through `FUN_004064fe`, and the adjacent open-conversation, tick-toggle, and timer-stop helpers `FUN_00406550`, `FUN_004067d0`, and `FUN_004067f0`. The isolated `science` probe now verifies the `0xb00` page layout, the recovered `0x90` template ctor order, the two 2-element series-vector bands, the three widget slots, the subject-conversation reopen path, the timer/tick helpers, and the corrected `+0x958c` sheet-root child span.
- `codex-science-cell-grid-controller`: Recovered the narrow Science Kit chemical cell-grid/controller corridor around `FUN_00408a00`, `FUN_00408bc0`, `FUN_00408ca0`, `FUN_00408cf0`, `FUN_00408d30`, `FUN_00408db0`, `FUN_00408e10`, `FUN_00408e50`, `FUN_00408eb0`, `FUN_00408ec0`, `FUN_00408ed0`, `FUN_00408ee0`, `FUN_00408f00`, `FUN_00408f20`, `FUN_00408f30`, `FUN_00408f70`, `FUN_00408fd0`, `FUN_00409050`, and `FUN_00409070` into `reconstruction/creatures_slice/src/science_cell_grid_page.c`, with the isolated `science` probe target extended to verify the 64x48 coordinate lattice, palette lift, snapshot capture/restore, channel-2 conversation reopen, fixed `inst,setv var0 1,endm` refresh dispatch, decoded response plotting, and full-surface invalidation.
- `codex-science-cell-grid-lifetime`: Recovered the nearby Science Kit cell-grid page lifetime thunks around `FUN_00408800`, `FUN_00408920`, `FUN_00408940`, and `FUN_004091e0` into `reconstruction/creatures_slice/src/science_cell_grid_page_lifetime.c`, with the isolated `science` probe target extended to verify the `0x86` page-template constructor path, the 3072 no-op point constructions, the embedded surface-owner init/destroy ordering, and the low-bit scalar-delete release path.
- `codex-science-sheet-base-lifetime`: Recovered the narrow Science Kit sheet-base/page-constructor cluster around `FUN_004075c0`, `FUN_004075e0`, `FUN_004076d0`, `FUN_00407740`, and `FUN_00407840` into `reconstruction/creatures_slice/src/science_sheet_base_lifetime.c`, with the isolated `science` probe target extended to verify the 32-bit object offsets, the dialog/registry/embedded-page ctor ordering, the fixed icon-resource init path, the base dtor ordering, and the two imported-ordinal scalar-delete wrappers.
- `codex-window-bootstrap-media`: Recovered the higher-level `Creatures.exe` window-media bridge around `FUN_00402950`, `FUN_00402980`, `FUN_004029b0`, `FUN_00402a10`, `FUN_00402a30`, `FUN_00402ab0`, and `FUN_00402b90` into `reconstruction/engine/src/creatures_window_media_bridge.c` with shared harness coverage for bridge init/bind, repeated backbuffer resize, palette realization, scoped/direct present, and release sequencing.
- `codex-ambient-chemistry`: Recovered the downstream creature-side ambient chemistry consumer around `FUN_004014b0` and `FUN_0041fb10` into `reconstruction/creatures_slice/src/creatures_ambient_chemistry.c` with a standalone `ambient_chemistry` probe target.
- `codex-creature-post-chemistry-chooser`: Recovered the narrowed post-chemistry chooser around `FUN_004226d0` into `reconstruction/creatures_slice/src/creatures_post_chemistry_chooser.c` with a standalone `post_chemistry_chooser` probe target, exact fallback tables lifted from the PE image, and the shared target-switch runtime widened so slot ids `1..40` plus the `-1` sentinel remain addressable.
- `codex-creature-target-slot-matrix`: Recovered the byte-oriented creature target-slot matrix helpers around `FUN_00414ec0`, `FUN_00421bd0`, `FUN_00421c20`, `FUN_00421dc0`, `FUN_00421e20`, and `FUN_004222e0` into `reconstruction/creatures_slice/src/creatures_target_slot_matrix.c` with a standalone `target_slot_matrix` probe target, making the 16-byte packed slot record explicit.
- `codex-creature-target-scan-loop`: Recovered the broader creature target-scan/update corridor around `FUN_00421f50` plus the immediate local behavior emitter helpers `FUN_004219b0`, `FUN_004219f0`, `FUN_0041fea0`, and `FUN_00421e90` into `reconstruction/creatures_slice/src/creatures_target_scan_loop.c` with a standalone `target_scan_loop` probe target, making the 20-byte behavior template table explicit and re-verifying the native and Windows `creatures` app builds.
- `codex-creature-wait-behavior-formatter`: Recovered the standalone wait-behavior formatter `FUN_00421430` into `reconstruction/creatures_slice/src/creatures_wait_behavior_formatter.c` with a standalone `wait_behavior_formatter` probe target, preserving the strongest-drive scan, the `0x40` low-drive fallback threshold, the special drive-`1` and drive-`7/8` formatting branches, and the empty-string fallback for unrecovered strong-drive cases.
- `codex-creature-behavior-status-display`: Recovered the adjacent creature behavior/status text corridor around `FUN_00420720`, `FUN_00420870`, `FUN_004209e0`, `FUN_004211e0`, `FUN_00421750`, `FUN_00421790`, and `FUN_00421830` into `reconstruction/creatures_slice/src/creatures_behavior_status_display.c` with a standalone `behavior_status_display` probe target, preserving the exact `0x36`-byte phrase-slot layout, the vowel/consonant stemmer, the first-match replacement ordering, the score-plus-five emitter path, the fixed-priority emitter path, and the family-`0x04` one-in-four suppression gate.
- `codex-creature-drive-state-helpers`: Recovered the narrow creature drive-state helper pair around `FUN_004218c0` and `FUN_00421990` into `reconstruction/creatures_slice/src/creatures_drive_state_helpers.c` plus the target-slot matrix API, with a standalone `drive_state_helpers` probe target covering the current-drive-slot transition helper, its slot-0 retry path, the family-`0x04` behavior-token fallback, and the exact 16-byte target-slot record initializer reused during creature construction.
- `codex-audio-sample-cache`: Recovered the `Creatures.exe` sound-sample cache/load path around `FUN_00408660`, `FUN_00408a30`, `FUN_004092a0`, `FUN_00409340`, `FUN_004093b0`, `FUN_00409410`, `FUN_00409520`, and `FUN_004095c0` into `reconstruction/engine/src/creatures_media_sound_cache.c` with shared harness coverage for file-size measurement, RIFF/WAVE/fmt/fact/data parsing, buffer-lost relock, split-lock uploads, LRU eviction, cache-hit serial refresh, miss load/append, budget reset, and suspend gating. Re-verified `./build/reconstruction_native/reconstruction_native`, `bash scripts/build_reconstruction_native.sh creatures`, and `bash scripts/build_reconstruction_windows.sh slice`.
- `codex-audio-voice-dispatch`: Recovered the narrow `Creatures.exe` playback/voice-dispatch bridge around `FUN_00408ef0`, `FUN_00409680`, `FUN_0040984a`, `FUN_00409860`, and `FUN_004098f0` into `reconstruction/engine/src/creatures_media_voice_dispatch.c` with a standalone `audio_voice_dispatch` probe target, restoring the hidden volume/pan inputs semantically and re-verifying native and Windows `slice` plus `creatures` app builds.
- `codex-audio-queue-dispatch`: Recovered the broader async/queueing playback callers around `FUN_004090a0`, `FUN_004096c0`, `FUN_00409950`, and `FUN_004099c0` into `reconstruction/engine/src/creatures_media_audio_queue_dispatch.c` with a standalone `audio_queue_dispatch` probe target, preserving immediate one-shot playback, delayed request countdown/dispatch, completion polling, and the 15-step fade versus hard-stop split above the recovered voice-dispatch layer.
- `codex-audio-spatial-object-wrappers`: Recovered the higher-level positional/object sound corridor around `FUN_00414b00`, `FUN_00414c60`, `FUN_00414da0`, `FUN_00419180`, `FUN_004191d0`, and `FUN_00419280` into `reconstruction/engine/src/creatures_media_spatial_sound.c` with a standalone `audio_spatial_sound` probe target, preserving wrapped audibility checks, pan/attenuation computation, one-shot spatial playback, managed offscreen restart arming, live mix refresh, and hard-stop versus fade teardown above the recovered queue and voice layers.
- `codex-creatures-app-build-reconciliation`: Reconciled the `creatures` app bootstrap/build edge outside the active archive/science/CAOS/target-scan slices: `reconstruction_probe_creatures_startup()` now treats the default genetics requirement as the configured genetics directory rather than a hard-coded `norn.gen`, `reconstruction/apps/creatures/main.c` now derives `--install-root` from `argv[0]` when no explicit override is passed, and the app/debug outputs were updated to the current archive-header `raw_tag` field names. Verified the startup probe against `build/reconstruction_native` with a standalone harness and removed the stale Windows target-slot drift blocker; the remaining full-app build failure is now the active embedded-object archive compile break documented below.
- `codex-viewport-capture-safety`: Hardened `creatures_capture_8bit_bitmap_region` / `creatures_capture_centered_viewport_region` in `reconstruction/engine/src/creatures_viewport_capture.c` to preserve top-down WinG row order, read bottom-up buffers correctly, reject out-of-bounds client rectangles, and expanded the shared slice harness coverage for top-down, bottom-up, centered, and submit-failure paths. Re-verified both `./build/reconstruction_native/reconstruction_native` and an AddressSanitizer slice build.
- `codex-creature-target-switch`: Recovered the shared creature target-switch helper around `FUN_00420f80` into `reconstruction/creatures_slice/src/creatures_target_switch.c` with a standalone `target_switch` probe target.
- `codex-body-coverage-reconciliation`: Reconciled the stale body-system notes that still advertised `FUN_0040e700`, `FUN_0040f900`, and `FUN_0040f922` as pending, added `notes/creatures-body-builder-reconstruction.md` as the canonical `FUN_0040f922` summary, and re-verified the shared native slice harness so future agents can treat the body layout/builder stack as already covered.
- `codex-creature-phrase-response-controller`: Recovered the adjacent creature phrase-response corridor around `FUN_004205e0`, `FUN_00420bd0`, `FUN_00421040`, and `FUN_00421250` into `reconstruction/creatures_slice/src/creatures_phrase_response_controller.c` with a standalone `phrase_response_controller` probe target, preserving the eighty-slot seed-row mapping, the space-tokenized input matcher, the intervening-peer chooser, the spontaneous phrase emitter, and the confidence-scaled handoff into the recovered target-switch helper.
- `codex-creature-speech-literal-tables`: Recovered the PE-backed speech literal tables behind the behavior/status and phrase-response slices into `reconstruction/creatures_slice/src/creatures_speech_literal_tables.c`, including the eighteen substring replacement pairs from `DAT_004369e8 .. PTR_DAT_00436a78`, the eight interleaved `8 x 5` seed rows from `PTR_DAT_00436908 .. PTR_DAT_00436918`, and the spontaneous-phrase category maps from `DAT_00436ab8` / `DAT_00436ac8`. The behavior/status and phrase-response modules now fall back to these recovered defaults when callers do not override the tables, and the standalone probes verify the PE-backed replacements, seed rows, and category routing directly.
- `codex-creature-voice-lexicon`: Recovered the creature voice/trigram helper cluster around `FUN_004283d0`, `FUN_00428530`, `FUN_004285e0`, `FUN_004287f0`, `FUN_004288f0`, and `FUN_004289a0` into `reconstruction/creatures_slice/src/creatures_voice_lexicon.c`, with a standalone `voice_lexicon` probe target covering constructor defaults, direct trigram matches, separator-band fallbacks, streaming scan offsets, and raw `.vce` payload roundtrips against the shipped `male.vce` table. Re-verified native and Windows `slice` plus `creatures` app builds.
- `codex-creature-voice-word-helpers`: Recovered the narrow creature voice word or phrase helper corridor around `FUN_004286d0`, `FUN_004287d5`, `FUN_004287e7`, `FUN_004288e0`, `FUN_0042890f`, and `FUN_00428994` into `reconstruction/creatures_slice/src/creatures_voice_runtime.c`, with a standalone `voice_runtime` probe target covering lowercase-plus-space phrase normalization, elapsed-ticks access, the stream-side `.vce` reader twin, and the tiny imported cleanup forwarders. This pass also corrected `creatures_voice_lexicon_load_file` so direct `.vce` loads now follow the decompiled file order instead of the separate archive-transfer order.
- `codex-creature-voice-membership`: Re-scoped the adjacent `FUN_004282d0` / `FUN_00419010` pair away from the voice lexicon itself and recovered it as the active-macro/world-object agent-token claim guard in `reconstruction/creatures_slice/src/creatures_agent_token_membership.c`, with a standalone `agent_token_membership` probe target covering the five-slot macro membership test plus the global availability scan over live world objects and active macros.
- `codex-creature-base-reset-thunks`: Recovered the generic base-object reset helper band around `FUN_004014a0`, `FUN_0042850d`, and `FUN_00428525` into `reconstruction/creatures_slice/src/creatures_base_reset_thunks.c`, with a standalone `base_reset_thunks` probe target covering the shared base-vtable overwrite helper, the tiny imported cleanup forwarder, and the matching stack-local unwind thunk. Re-verified native and Windows `slice` plus `creatures` app builds.
- `codex-creature-drive-slot-chooser`: Recovered the neighboring drive-slot chooser/reset corridor around `FUN_00422320`, `FUN_00422630`, and `FUN_00422670` into `reconstruction/creatures_slice/src/creatures_drive_slot_chooser.c`, with a standalone `drive_slot_chooser` probe target covering randomized variant selection, category-6 slot choice, follow-state reset, spontaneous phrase dispatch, and the pending pairing-token handoff/acceptance path. Re-verified native and Windows `drive_slot_chooser`; the aggregate native `slice` build is currently blocked outside this slice by an unrelated `creatures_eye_window.c` compile error.

### Known Gaps / Open Questions:
- The shared Health Kit bitmap-strip resource and palette-builder helpers are now covered, but the higher-level local wrapper/page-owner seam around `FUN_00409540`, `FUN_00409587`, `FUN_00409af0`, `FUN_00409bf0`, `FUN_00409c10`, `FUN_00409cf0`, `FUN_00409d40`, and the nearby `FUN_0040a0aa` / `FUN_0040a0b5` / `FUN_0040a0ca` wrappers still needs a separate pass above the recovered resource runtime.
- The imported-creature picker now includes the recovered stack dialog runtime plus its embedded `+0xb0` string and base destroy ordering, but the actual imported MFC constructor, `DoModal()`, and path-extraction methods are still abstracted through dialog ops rather than a fully concrete portable `CFileDialog` runtime.
- The recovered Launcher-local bitmap/palette, controller shell, and adjacent window-init/draw/on-exit bands now meet cleanly through `FUN_0040262b`; the next Launcher-focused follow-on is the higher lifecycle/static-construction seam around `FUN_00402db0`, `FUN_00402e1f`, `FUN_00402e30`, `FUN_00402e77`, `Ordinal_4933`, `FUN_00402fb0`, `FUN_00402fd0`, `FUN_0040305b`, and `FUN_00403070`.
- The recovered Science Kit bitmap-buffer runtime and the higher-level bitmap-presenter host now meet cleanly through the local file-or-blank load, palette/DIB rebuild, centered layout, and clipped blit path. The remaining nearby Science-local follow-on is the broader page/controller layer above this host seam rather than the recovered `FUN_0040f220`, `FUN_0040f410`, `FUN_0040f550`, `FUN_0040f9d0`, `FUN_0040fbf0`, `FUN_0040fd00`, `FUN_0040fe10`, `FUN_0040fe60`, and `FUN_00410050` band.
- Aggregate native and Windows `slice` builds are currently passing again on the host; the remaining nearby Breeder follow-on is the deeper `+0x265c` child-panel work rather than a current build blocker in this corridor.
- The generic Health Kit indexed-bitmap host/object seam is now covered, but the higher-level Health page/controller callers that own these bitmap hosts still need distinct recovery work above the recovered host runtime; the nearby `FUN_004088c0` remains intentionally claimed by the already verified chemistry-gauge base cleanup rather than the new bitmap-host module.
- The staged asset tree is now sufficient for the raw sprite preview and the deeper body-bank preview, but not yet for a full assembled creature-body layout: the recovered graph-loader still expects live `.qad` anchor tables for the appendage chains, and there are no shipped `.qad` files anywhere in the current staged install tree or extracted media.
- The recovered Owner session/registry, shell/controller, support-object, page-object lifetime, photo-album page-controller, photo-album loader, register-page controller, cross-page bridge, temporary-cleanup family, bitmap-surface runtime, Owner-local OLE/runtime helpers, top-level Owner tool controller, the deeper embedded-member lifetimes, their split cleanup wrappers, the remaining photo-album/monitor unwind ladders, and the imported runtime-stage identities now meet cleanly. The remaining nearby Owner-local follow-on is higher-level page/controller coverage elsewhere in the kit, not this runtime band.
- The recovered Breeder window/session helper band now also includes the adjacent command/response corridor, the nearby lifecycle/cleanup family, the bitmap-entry/runtime wrappers beneath that window/session layer, the matching HKCU/HKLM helper wrappers above the embedded shared-registry runtime, the local `+0x238` child-shell lifetime band, the next higher-level root/controller bridge above that shell, the concrete `+0x3168` add-object page beneath that root seam, and the full `+0x265c` child-panel controller/runtime seam. That combined coverage now includes persisted window placement, subject-handle acquire/release, subject-name refresh, the missing-subject retry prompt, local/remote quit dispatch, command execution, response release, startup control-message routing, the local constructor/destroy cleanup chain, bitmap-entry archive/blit forwarding, runtime open/read/write/default registry access, the embedded shared-registry lifetime, the recovered child-shell ctor/dtor ordering above those helpers, base/root ctor-dtor order, startup timer re-arm, child refresh toggles, subject moniker refresh, activation gating, the on-exit registration helper band, archive-backed shop-item load/save, the `Addbgd.bmp` plus `Shop.bmp` preview redraw path, the `+0x265c` conversation gate, palette-backed background attach, pregnancy preview asset refresh, and live animation tick/redraw. The remaining nearby Breeder-local follow-on is now the separate active `+0x3168` child/controller seam, not the closed `+0x265c` panel band.
- The live `World.sfc` boot path currently reaches the embedded-object stream and decodes the first live anchored octet: `PointerTool`, `Entity`, `CompoundObject`, `SimpleObject`, `Vehicle`, `Blackboard`, `Lift`, and `CallButton`. The broader embedded-object parser is still unresolved beyond that point.
- The byte-level post-`CallButton` corridor is now pinned through EOF, so the remaining embedded-object blocker is owner semantics rather than local archive seams: the strongest root candidate is still `FUN_004056b0`, with `DAT_00436ea4 + 0x9c/+0xe0` now pinned as a six-slot `_Camera` bookmark cache and `DAT_00436ebc + 0x80/+0x84` as the separately archived selected-creature recent-history/status child.
- The `Incubator`-adjacent tail now has a stronger child-level owner candidate too: `DAT_00436ea8` owns a ten-slot `0xae` external-tool/DDE catalog at `+0xc4`, per-slot transport state at `+0x16a + slot * 0xae`, and timer/command traffic through its own `HWND` at `+0x20`; that slot geometry is now tighter too, because `+0x6da` and `+0x788` resolve exactly to slot `8` and slot `9` transport bytes while `+0x790` is the live pending-launch count immediately after the ten-slot array. The paired `DAT_00436eac` object now looks more like the redraw host those updates invalidate than the serialized tail owner.
- The generic DDE service root under that child is now separated too: `DAT_00438bc0` is the stronger current service-name/topic-registration object, while `DAT_00436ea8` consumes that layer to manage live external-tool slots and the `Incubator`-adjacent tail.
- The late post-`Scenery` tail is no longer anonymous either: the `289`-entry `u32 key + CString` band matches the archived macro-name registry (`DAT_00436c24` / `DAT_0043912c`), and the next stable seam at `0x28072` / `0x28076` is now the strongest current active-`Macro` band candidate because it begins with a proven `ReadObject(new class "Macro")` seed.
- The trailing `FUN_00424410` / `FUN_00424640` block is recovered as a serialized classifier-script catalog, and the embedded-object parser now handles long CAOS `CString` payloads instead of the earlier 63-byte cap.
- The first live `CompoundObject` tail now decodes through:
  - `part_count: 4` at `0x2a04`
  - four inline `ReadObject(Entity)` part payloads plus their two trailing dwords
  - the six raw `16`-byte hotspot blocks
  - the six trailing hotspot/state dwords
- The widened anchored-class scan now also reaches the next immediately following class boundary:
  - `anchored_class[3]: 0x2b2e SimpleObject`
  - `anchored_object[3]: kind=3 bytes=0x3ac classifier=0x02090100`
  - `simple_entity_tag=0x8008`
  - `animation_group=0x000001f4`
  - `serialized_scripts=2`
- The embedded-object bootstrap now also decodes the following live `Vehicle`:
  - `anchored_class[4]: 0x3ea2 Vehicle`
  - `anchored_object[4]: kind=6 bytes=0x420 classifier=0x03010100`
  - `vehicle_motion0=0x00000000`
  - `route_state=0x00000000`
  - `serialized_scripts=3`
- The same live bootstrap now also decodes the following anchored classes:
  - `anchored_class[5]: 0x722c Blackboard`
  - `anchored_object[5]: kind=8 bytes=0xaf3 classifier=0x03030100`
  - `blackboard_state=ff-ff-ff-ff-ff`
  - `populated_entries=16`
  - `anchored_class[6]: 0x10852 Lift`
  - `anchored_object[6]: kind=7 bytes=0x381 classifier=0x03020100`
  - `lift_motion0=0x00000000`
  - `route_state=0x00000200`
  - `serialized_scripts=4`
  - `anchored_class[7]: 0x10bd3 CallButton`
  - `anchored_object[7]: kind=4 bytes=0xca classifier=0x02020000`
  - `callbutton_target=0x0001`
  - `button_state=50`
  - `serialized_scripts=2`
- The remaining embedded-object uncertainty is now narrower and more structural:
  - the top-level `Entity` at `0x25f2` and the inline `Entity` references under `CompoundObject` do not share the same serialized body shape
  - the exact original function owning the smaller inline `20/52` byte `Entity` reference payload is still not pinned to a named lift
  - `Lift` is now confirmed as a `Vehicle`-compatible archive body plus a fixed `0x3a` trailing raw tail before the next class header
  - a partial `FUN_0040e090` creature-pose/body-layout archive reader now exists, and the specialized `FUN_0041a060` / `FUN_0041a8d0` pose-part records beneath it are now modeled in the reconstruction
  - the live post-`CallButton` probe now resolves more than a raw object tag at the currently inferred offset:
    - `post_callbutton_offset: 0x10c9d`
    - `post_callbutton_pose: decode_failed`
    - `post_callbutton_part: bytes=0x32 ref=0x0081 state=00-02 d10=0x00040002 d0e=0x00000000 d12=0xa0000000 optional=20 next=0x10ccf`
    - `post_callbutton_scripts: zero=0x00000000 count=2 key0=0x02020001 text0=pose 1,snde butt,endm key1=0x02020000 text1=pose 0,snde butt,endm next=0x10d0b`
    - `post_callbutton_next_tag: raw=0x8008 kind=3 object=0 class=8`
    - `post_callbutton_tag_probe: raw=0x0081 kind=1 object=129 class=0`
  - the bytes immediately after that decoded pose-part record are no longer just “CAOS-looking”; they now decode as a real two-entry classifier-script catalog, so the next pass needs to identify the surrounding container/object stream rather than treating `0x10c9d` as a standalone new-class boundary
  - a second repeated live family now sits farther forward at `0x10d29` and repeats at `0x111d0`, `0x115ea`, `0x11b1e`, and `0x12052`; it appears to begin with another `FUN_0041a060`-shaped record and then reaches a four-entry classifier-script catalog keyed to `0x03020100` at `0x10d60`
  - the decoder-driven scan now also pins down the first repeated-family boundary:
    - `post_callbutton_repeated_part_offset: 0x10d29`
    - `post_callbutton_repeated_part: bytes=0x32 ref=0x812c state=00-01 d10=0x0c000302 d0e=0x00000000 d12=0x00000000 optional=0xa0`
    - `post_callbutton_repeated_scripts: offset=0x10d60 count=4 key0=0x03020100 ... next=0x10efb`
  - the stream at `0x10efb` is now typed one layer deeper too:
    - `post_callbutton_repeated_chain_list: offset=0x10efb count=3 outer0=0x8008 inner0=0x012e d10_0=0x000003e8 tail0=0x00000000 next=0x10f50`
    - `post_callbutton_repeated_pair_table: offset=0x10f50 d0=80 d11=82 d24=2 d29=-1 next=0x10fc8`
    - `post_callbutton_repeated_quad0: offset=0x10fc8 d0=0 d3=242944 d9=3 next=0x10ff0`
    - `post_callbutton_repeated_quad1: offset=0x10ff0 d0=2 d3=0x9f000000 d9=0 next=0x11018`
    - `post_callbutton_repeated_next_family: 0x111d0`
    - this confirms a counted list of wrapped `FUN_0041a8d0`-shaped records followed by fixed-size body-sprite table payloads rather than another raw false-positive class-header scan
  - the mid-band gap after those fixed body-sprite tables is now decoder-pinned too:
    - `post_callbutton_repeated_scripted_part[0]: offset=0x1102a ref=0x0081 key0=0x02020001 next=0x11098`
    - `post_callbutton_repeated_scripted_part[1]: offset=0x110b7 ref=0x0081 key0=0x02020001 next=0x11125`
    - `post_callbutton_repeated_scripted_part[2]: offset=0x11144 ref=0x0081 key0=0x02020001 next=0x111b2`
    - each of the three records is another `FUN_0041a060`-shaped part followed by `u32 0` and a two-entry classifier-script catalog using the same `pose 1,snde butt,endm` / `pose 0,snde butt,endm` pair as the earlier `CallButton` corridor
  - a naive raw `find_next_new_class_header()` scan remains noisy after `CallButton` and starts returning false positives like class name `2`, so the next pass still needs decoder-driven stream walking rather than another raw header scan
- The higher-level `creatures` preview path now has room-label, annotated, hatchery, incubator, scenery, machinery, and ambient scene modes above the same recovered world-header and packed body-bank seam. The next safe render-only follow-on above that layer is richer transport or utility composition rather than deeper world boot, which is still gated by the active embedded-object archive task.
- `MapData` room/climate helper semantics are still partially inferred from callsites (`TEMP`, `WIND`, room-span queries) and static tables, but the higher-level ambient weather-state writer band above them is now recovered into `creatures_map_data_weather_state`.
- The creature-side ambient consumer, the sibling post-chemistry chooser, the byte-oriented target-slot matrix helpers, the broader target-scan/update corridor, and the wait-behavior formatter are now recovered, but the full `CBiochemistry` object layout is still normalized rather than byte-accurate and the relationship fields plus behavior-template flags in the new target-scan slice are still semantic rather than byte-accurate.
- The neighboring drive-slot chooser and pairing-handoff corridor around `FUN_00422320`, `FUN_004224c0`, `FUN_00422630`, and `FUN_00422670` is now recovered: `+0x2b74` is the 32-bit `baby` token slot while `+0x2b78` remains the one-byte `dead` flag, and `FUN_004224c0` is now pinned as the local writer that stages or clears the parent genome token in `+0x2b70` before the recovered `f**k` handoff consumes it.
- The neighboring genome document/archive helper band is now covered, and the earlier `FUN_0041be70` mention in that handoff was stale because the parser-side record seeker was already recovered separately.
- The tiny `CBrain` runtime-class bridge `FUN_00429e80`, the bounded-byte body-gene profile readers around `FUN_00429ea0`, `FUN_00429ed0`, `FUN_00429f40`, `FUN_0042a120`, `FUN_0042a170`, and `FUN_0042a1b0`, the recovered variant-layout consumer band around `FUN_0042a2f0`, `FUN_0042ba10`, `FUN_0042bbb0`, and `FUN_0042bc20`, the duplicate runtime-layout entry and owner lifecycle band around `FUN_0042a30f`, `FUN_0042a6a0`, `FUN_0042a750`, `FUN_0042a770`, `FUN_0042a7c1`, and `FUN_0042a7e4`, the sample mutation/export helper band around `FUN_0042bde0`, `FUN_0042be10`, `FUN_0042be60`, `FUN_0042beb0`, `FUN_0042bef0`, `FUN_0042bfc0`, and `FUN_0042c210`, the next effect-runtime consumers `FUN_0042c3d0` and `FUN_0042c500`, the selector/controller follow-ons `FUN_0042c2c0`, `FUN_0042cf10`, `FUN_0042d000`, and `FUN_0042d2a0`, and the post-selector state helpers `FUN_0042d2e0` and `FUN_0042d380` are now recovered into focused modules. There are no standalone body-gene-side gaps currently tracked.
- The creature voice/trigram lexicon tables, the adjacent voice phrase runtime, the packed `.vce` token semantics, the nearby agent-token claim guard, the generic base-object reset thunks, and the higher-level spatial/object sound wrappers are now recovered. There are no standalone voice-side gaps currently tracked.
- The tiny component-easing helper family around `FUN_00428b30` and `FUN_00428b80` is now recovered into a focused module, and the larger body-gene effect runtime consumers plus their selector/controller follow-ons are also lifted. The remaining work around that corridor is no longer in the body-gene helper band; it sits elsewhere in the selected-creature and archive-adjacent flows, not the eye-window layer.
- The eye-window lifetime layer above the recovered `EyePosn` save helper and display-surface primitives is now recovered, and the follow-update pair `FUN_004290b0` / `FUN_004290bb` now tracks selected-creature bounds or cached eye-focus coordinates with wrapped horizontal easing. The remaining eye-window-adjacent gaps are elsewhere in the selected-creature UI flow rather than the separate body-gene effect runtime below the variant-profile readers.
- The adjacent boot-registry privilege/init band, boot-sequence wrappers, startup-tip corridor, and timer-suspended command wrappers around `FUN_00429270`, `FUN_00429360`, `FUN_00429540`, `FUN_00429860`, `FUN_00429bf0`, `FUN_00429c4f`, `FUN_00429d60`, and `FUN_00429d80` are now recovered into focused engine modules. The previously open imported-ordinal identity gap is closed: the wrappers now map to `CWinApp::OnFileNew`, `CWinApp::OnFileOpen`, `CDocument::OnFileSave`, and `CDocument::OnFileSaveAs`.
- The `creatures` app startup probe now resolves the packaged `build/reconstruction_native` asset tree without an explicit `--install-root`, and both native and Windows `creatures` app targets compile again; the remaining launch blocker stays on the live `World.sfc` embedded-object boot path rather than the target-scan corridor.
- The broader CAOS interpreter at `FUN_00426670` is now recovered into a standalone dispatcher slice, but several opaque object/audio/creature command arms inside it still route through injected callbacks until their owning subsystems are lifted byte-accurately.
- The shared helper-kit registry layer, the first Science Kit DDE command builders, the narrow dosage-page controller cluster, the subject/session gate, the adjacent window/session lifecycle corridor, the sheet-base lifetime cluster, the base embedded page-host band at `+0xb0`, the sheet-root lifetime cluster, the next sheet runtime/controller band, the `+0x7fa4` Decision page, the larger `+0x958c` chemical-graph page band, the chemical cell-grid controller, the nearby cell-grid lifetime thunks, and the full local `+0x238` monitor-page shell plus its init/update/redraw, trace/frame, and cleanup-wrapper corridors are now recovered. The remaining kit work above them is narrower now: the broader Science/Owner/Health sheet-controller layers still need separate, non-overlapping passes.
- The isolated Health Kit monitor-page animation/update corridor, its adjacent snapshot-restore helper, the next higher-level Health sheet base/root lifetime-runtime band, the four recovered root child-shell lifetime bands, the full `+0x2b78` lobe-page/controller corridor, the full `+0x3eb4` add-object page/controller and presenter corridor, the full `+0x10f8` chemistry-page/controller plus lower support-object band and the concrete five-entry chemistry-gauge presenter vector beneath it, the recovered `+0x94` base-sheet registry helper, and the top-level root activation/fanout seam are now recovered. The remaining Health-side follow-on is now any still-unrecovered shell/message bridge or top-level message-routing work above those layers, not the `+0x10f8` child gauge band itself.
